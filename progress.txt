# v7 Ralph 루프 진행 기록 (prd-v7-filters-presets.md)

## Iteration 1 — US-1. 데이터 수집 스크립트 12개월 전환
- 완료 스토리: US-1
- 변경 파일: scripts/collect_data.py (PERIOD "6mo"→"12mo", MIN_TRADING_DAYS 110→200, 주석 갱신)
- 테스트 수 변화: 없음 (Python 스크립트는 vitest 대상 아님, npm run test 102개 유지 확인)
- 운영자 액션 필요: collect_data.py 재실행 후 nasdaq100.json 재수집·커밋 필요 (12개월치로 갱신).
  이 루프는 스크립트를 실행하지 않음 — python 환경(yfinance/pandas)이 있는 별도 세션에서
  `python scripts/collect_data.py` 실행 후 결과 JSON을 커밋해야 US-2 이후 스토리가
  실데이터 기준으로 검증 가능해짐. 단, US-2~US-8은 순수 함수/픽스처 테스트라 재수집 없이도
  구현·검증 가능하므로 루프는 계속 진행함.
- 다음 스토리 메모: US-2 (sixMonth 차트 정의를 "전체 기간" → "최근 126거래일"로 변경,
  deriveTickerData.js 수정 + 200일/120일 픽스처 테스트).

## Iteration 2 — US-2. sixMonth 차트 정의 변경 (수집 기간 변경 파급효과 차단)
- 완료 스토리: US-2
- 변경 파일: src/lib/deriveTickerData.js (SIX_MONTH_WINDOW=126 상수 추가, chart.sixMonth를
  series.slice(-126)로 변경), src/lib/deriveTickerData.test.js (200일/120일 합성 시리즈
  테스트 3개 추가)
- 테스트 수 변화: 102 → 105 (+3), 전체 통과. 실데이터(nasdaq100.json, 현재 123거래일)는
  126 이하라 기존 "sixMonth === 전체 series" 어서션이 우연히 계속 참 — 실회귀 없음 확인.
  npm run build 통과.
- 운영자 액션 필요: 없음 (이 스토리는 순수 함수 변경, 재수집 불필요)
- 다음 스토리 메모: US-3 (indicators.js에 bollingerBands, week52HighLow 추가 —
  기존 함수 수정 금지, 252일 미만 시 week52HighLow는 null 반환).

## Iteration 3 — US-3. 신규 지표 함수 1차 (볼린저밴드 + 52주 신고가/신저가)
- 완료 스토리: US-3
- 변경 파일: src/lib/indicators.js (bollingerBands, week52HighLow 추가 — 기존 함수 무수정),
  src/lib/indicators.test.js (수기계산 대조 6개 테스트 추가)
- 설계 결정: PRD가 두 함수 모두 파라미터명을 `series`로 명명한 것에 착안해, 기존 6개 함수와
  달리 순수 종가 배열이 아니라 원본 바 객체 배열({date,high,low,close,volume})을 입력받도록
  통일함 — 52주 고저는 high/low 컬럼이 필요하고, 이후 US-4의 stochastic/atr도 동일하게
  bar 배열을 받을 예정이라 v7 신규 지표 5종의 시그니처를 여기서 맞춰둠. 두 함수 모두
  "현재 시점 스냅샷 값"만 반환(배열이 아님) — 필터 판정에는 최신값만 필요하기 때문.
- 테스트 수 변화: 105 → 111 (+6), 전체 통과. npm run build 통과.
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-4 (stochastic/atr/obv 추가, series bar 배열 입력 시그니처를
  US-3과 동일하게 유지. 스토캐스틱 분모 0, ATR Wilder 평활 초기값, OBV 보합일 케이스 필수).

## Iteration 4 — US-4. 신규 지표 함수 2차 (스토캐스틱 + ATR + OBV)
- 완료 스토리: US-4
- 변경 파일: src/lib/indicators.js (stochastic, atr, atrPercent, obv + private helper
  smaAllowingNulls 추가), src/lib/indicators.test.js (10개 테스트 추가)
- 설계 결정: obv()는 다른 v7 신규 지표(스냅샷 반환)와 달리 **배열**을 반환함 — PRD 명세
  그대로("누적 OBV 배열"), 필터가 "OBV vs OBV의 SMA20"을 비교하려면 과거 흐름 전체가
  필요하기 때문. stochastic은 분모 0(고가=저가)이 발생한 날의 Fast %K를 null로 두고,
  smaAllowingNulls 헬퍼로 그 null이 평활(SMA) 구간에 전파되게 함 — 억지로 스킵해
  결측을 감추지 않음.
- 테스트 수 변화: 111 → 121 (+10), 전체 통과. npm run build 통과.
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-5 (constants.js 임계 상수 + filters.js 판정 함수 5종.
  ATR 백분위는 유니버스 dataSufficient 종목 전체 분포 기준 — 개별 종목 함수가 아니라
  풀 단위 계산이 필요함에 유의. 52주 필터는 제외 종목 수도 함께 반환해야 함).

## Iteration 5 — US-5. 필터 판정 로직 + 임계 상수
- 완료 스토리: US-5
- 변경 파일: src/lib/constants.js(신규, 7개 상수), src/lib/filters.js(passesBollinger,
  passesWeek52, countWeek52Excluded, passesStochastic, passesAtrPercentile, passesObv 추가
  — 기존 applyFilters·DEFAULT_FILTER_STATE 무수정), src/lib/filters.test.js(17개 테스트 추가)
- 설계 결정: 판정 함수는 티커 전체 객체가 아니라 **이미 계산된 지표 값**(bands, week52,
  stoch, atrPercentValue, obvArray)을 인자로 받는 순수 함수로 설계 — deriveTickerData.js에
  이 v7 지표들을 실제로 채워 넣는 배선(스크리닝 UI와 실데이터 연결)은 아직 어느 스토리에도
  명시돼 있지 않으므로, 이후 US-7(화면1 UI 재편) 시점에 필요하면 그때 추가하기로 하고
  이번 스토리에서는 손대지 않음(범위 최소화). ATR 백분위는 percentileRank(value,population)
  = (value 이하 개수)/전체×100 방식으로 계산 — population=[1..10]일 때 value=3→30,
  value=7→70이 정확히 경계에 오는 성질을 이용해 경계 테스트를 구성함.
- 테스트 수 변화: 121 → 138 (+17), 전체 통과. npm run build 통과(번들 크기 변화 없음 —
  아직 App에서 import되지 않는 순수 라이브러리 코드이므로 트리셰이킹됨, 정상).
- 운영자 액션 필요: 없음
- ⚠ 다음 스토리(US-6) 착수 전 확인사항: deriveTickerData.js에 v7 지표(bollinger/week52/
  stochastic/atrPercent/obv) 필드가 아직 없다는 점을 인지할 것. US-6은 localStorage
  스키마 v2(필터 상태 모델)만 다루므로 이 갭과 무관하게 진행 가능하지만, US-7(화면1 UI)
  구현 시점에는 반드시 deriveTickerData.js 확장이 선행되어야 신규 필터가 실제로 동작함
  — U-7 스토리 안에서 처리하거나, 필요시 진행 기록에 새 서브 작업으로 남길 것.
- 다음 스토리 메모: US-6 (필터 state에 신규 5종 추가 + persistence.js 스키마 v1→v2
  마이그레이션. 기존 선택종목/비중/필터 보존 + customParams 범위검증 + researchRequests
  유니버스 필터링).

## Iteration 6 — US-6. 필터 상태 모델 + localStorage 스키마 v2
- 완료 스토리: US-6
- 변경 파일: src/lib/filters.js(DEFAULT_FILTER_STATE에 bollingerState/week52State/
  stochasticState/atrState/obvState 5개 필드 추가, 전부 기본 'off' — applyFilters 로직은
  무수정), src/lib/constants.js(CUSTOM_PARAM_RANGES 추가), src/lib/persistence.js
  (SCHEMA_VERSION 1→2, preset/customParams/researchRequests 필드 추가, v1→v2 마이그레이션
  로직 — 기존 field 보존 + 신규 field 기본값), src/lib/persistence.test.js(4개 테스트 추가)
- 설계 결정: schemaVersion이 SCHEMA_VERSION(2)도 MIGRATABLE_FROM_VERSION(1)도 아니면
  기존과 동일하게 완전 리셋(회귀 없음 확인 — 999 케이스 테스트 유지). customParams는
  필드 단위로 범위 검증해 범위 밖 필드"만" 기본값으로 되돌리고 나머지 유효한 필드는
  보존(완전 리셋 아님) — 스펙 "해당 값만 기본형으로"를 문자 그대로 구현.
- 테스트 수 변화: 138 → 142 (+4, persistence 5→9), 전체 통과. npm run build 통과.
- 운영자 액션 필요: 없음
- ⚠ US-5에서 남긴 갭 계속 유효: deriveTickerData.js에 v7 지표(bollinger/week52/
  stochastic/atrPercent/obv) 필드 없음. DEFAULT_FILTER_STATE·persistence에 신규 필터
  상태 필드는 이제 있지만, applyFilters()는 아직 이 신규 필드들을 사용하지 않는다
  (판정 함수는 US-5에 있지만 배선 안 됨) — US-7에서 화면1 UI를 재편할 때 반드시
  deriveTickerData 확장 + applyFilters 배선을 함께 처리해야 신규 필터가 실제로 동작함.
- 다음 스토리 메모: US-7 (화면1 필터 5그룹 접이식 재편. 위 갭 때문에 이 스토리 범위가
  원래 PRD 기술보다 커질 수 있음 — deriveTickerData에 v7 지표 계산 추가 + applyFilters에
  신규 5종 필터 판정 배선까지 포함해야 "회귀 테스트"와 "판정 함수와 결과 일치" 승인 기준을
  충족 가능. 기존 필터 4종 로직·배치 외 변경 금지 원칙은 유지).

## Iteration 7 — US-7. 화면1 필터 그룹핑 UI 재편 (+ US-5/6에서 남긴 배선 갭 해소)
- 완료 스토리: US-7
- 변경 파일:
  - src/lib/deriveTickerData.js: indicators에 bollinger/week52/stochastic/atrPercent/obv
    5개 필드 추가(기존 필드 무수정, 추가만) — v7 지표를 실제로 각 종목에 채워 넣는 배선.
  - src/lib/buildDataset.js: dataset.universeAtrPercents(유니버스 dataSufficient 종목의
    ATR% 모집단) 추가 — ATR 백분위 필터가 풀 단위 계산이 필요하기 때문.
  - src/lib/filters.js: applyFilters()에 신규 5종 필터를 기존 4종 뒤에 이어 붙임
    (4번째 인자 universeAtrPercents 추가, 기본값 [] — 기존 호출부 하위 호환).
  - src/App.jsx: applyFilters 호출에 dataset.universeAtrPercents 전달, week52ExcludedCount
    계산(countWeek52Excluded) 추가해 HomeSearch에 전달.
  - src/screens/HomeSearch.jsx: 필터 영역을 PRD §4.2 표 그대로 5개 접이식 그룹(모멘텀/
    추세·가격위치/거래량/변동성/시장구조)으로 재편. 그룹 헤더에 활성 필터 개수 배지.
    52주 필터 켜짐 시 "데이터 12개월 미만 종목 N개 제외됨" 안내.
  - 신규 테스트: filters.test.js에 applyFilters×v7필터 통합 6개, HomeSearch.test.jsx
    신설 7개.
- 갭 해소 확인: US-5/US-6에서 남긴 "deriveTickerData에 v7 지표 필드 없음" 갭을 이번
  스토리에서 해소함 — 더 이상 진행 기록에 남길 갭 없음.
- 테스트 수 변화: 142 → 155 (+13), 전체 통과(실데이터 e2e 포함 — week52는 현재 123일
  데이터라 전부 null, 정상). npm run build 통과(번들 178.68kB→185.71kB, 신규 UI 반영).
- 운영자 액션 필요: 없음 (52주 필터는 12개월 데이터가 실제로 재수집되기 전까지는 모든
  종목이 제외 상태로 동작 — US-1에서 이미 기록한 운영자 액션과 동일 건).
- 다음 스토리 메모: US-8 (recommend.js 설정 객체화 + presets.js 신설. 핵심 회귀 기준:
  기본형 설정 실행 결과가 리팩터링 전과 완전 동일해야 함 — 실데이터 스냅샷 비교 테스트
  필수. 2단계 배점(60/30/10)은 설정 대상 아님, v6 US-1 CLI도 계속 기본형으로 동일 결과
  내야 함에 유의).

## Iteration 8 — US-8. recommend.js 설정 객체화 + 프리셋 정의
- 완료 스토리: US-8
- 변경 파일:
  - src/lib/deriveTickerData.js: goldenCross3/goldenCross6/goldenCross20 필드 추가
    (기존 goldenCross5/10은 무수정) — 보수형(3→6 완화)·공격형(10→20 완화) 프리셋이
    필요로 하는 골든크로스 창. macdLine/signalLine 배열은 이미 함수 내부 스코프에
    있었으므로 별도 아키텍처 변경 없이 goldenCrossWithin() 호출만 추가.
  - src/lib/presets.js(신설): PRESETS.{conservative,default,aggressive} — RSI 하한/
    골든크로스 창(기준·완화)/고득점 편입 임계, 2단계 배점(60/30/10)은 포함하지 않음.
  - src/lib/recommend.js: recommend(tickers, config = PRESETS.default)로 시그니처 변경.
    stage1Pass/buildReasons가 하드코딩 상수 대신 config를 읽도록 리팩터링. 골든크로스
    창(숫자) → indicators 필드명은 GOLDEN_CROSS_FIELDS 매핑 테이블로 변환.
  - src/lib/recommend.test.js: (a) 리팩터링 직전에 저장해 둔 실데이터 스냅샷
    (__fixtures__/recommend-default-baseline.json, node -e로 커밋 전 캡처)과 config
    없이 호출한 결과를 deep-equal 비교하는 핵심 회귀 테스트, (b) 보수형/공격형 각각의
    RSI 임계·골든크로스 창·고득점 편입 임계·완화 폴백(창 2배) 테스트 7개 추가.
  - 설계 결정: config 없이 호출 시 PRESETS.default가 자동 적용되므로 App.jsx/
    select-pool.mjs(v6 US-1 CLI)/기존 recommend.test.js 12개는 전혀 손대지 않고도
    100% 기존 동작 유지 — 이번 스토리에서 호출부 변경은 0건.
- 테스트 수 변화: 155 → 162 (+7, recommend 12→19), 전체 통과. npm run build 통과
  (186.51kB). select-pool.mjs CLI 수동 재실행으로 AXON 80.1/MRNA 77.1 등 기존 수치와
  완전히 동일함을 재확인.
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-9 (화면2 프리셋 세그먼트 UI + 배너 연동. Recommend.jsx에 프리셋
  선택 세그먼트 추가, App.jsx의 recommend() 호출에 uiState.preset→config 매핑 연결
  필요 — 단 preset='custom'인 경우의 실제 파라미터 소스는 US-10의 customParams이므로,
  US-9 시점에는 3개 프리셋 전환만 우선 배선하고 custom 연동은 US-10에서 완성하는 편이
  자연스러움. "리서치 풀은 기본형 기준" 보조 문구도 이 스토리 범위).

## Iteration 9 — US-9. 화면2 프리셋 세그먼트 + 배너 연동
- 완료 스토리: US-9
- 변경 파일:
  - src/screens/Recommend.jsx: 상단에 보수형/기본형/공격형 세그먼트(버튼 3개, 클릭 시
    onPresetChange(key)) + 프리셋 한 줄 설명 + "사용자 설정" 상태 배지(preset==='custom'일
    때만 표시 — US-10 전까지는 도달 불가능한 상태). 완화 폴백/신호부족 배너 문구에
    프리셋 라벨(또는 "사용자 설정") 삽입. 리서치 섹션이 있는 카드에 preset이 기본형이
    아니면 "리서치 풀은 기본형 기준으로 선정되었습니다" 보조 문구 추가.
  - src/App.jsx: `activeConfig = PRESETS[uiState.preset] ?? PRESETS.default`를 계산해
    recommend(filteredTickers, activeConfig)로 전달 — recommendation useMemo 의존성에
    activeConfig 추가. onPresetChange로 uiState.preset을 갱신(US-6 스키마 필드,
    자동으로 localStorage에 저장됨).
  - 설계 결정: preset==='custom' 분기는 App.jsx에 아직 만들지 않음(US-10 전까지 UI로
    도달 불가능한 상태이므로) — uiState.preset이 'custom'이어도 PRESETS['custom']이
    undefined라 `?? PRESETS.default`로 안전하게 기본형 폴백됨. US-10에서 고급 설정
    패널을 추가할 때 이 분기와, recommend.js의 GOLDEN_CROSS_FIELDS 이산 매핑을
    임의 창(1~20) 계산으로 일반화하는 작업을 함께 처리해야 함(US-8에서 남긴 메모와 동일).
  - 신규 테스트: Recommend.test.jsx에 세그먼트 클릭/회귀/보조문구/배너문구 4개,
    App.test.jsx에 실데이터 기반 프리셋 전환 시 추천 결과(RSI 사유 텍스트)가 실제로
    달라지는 통합 테스트 1개.
- 테스트 수 변화: 162 → 167 (+5), 전체 통과(실데이터 e2e 포함). npm run build 통과
  (186.51kB→187.75kB).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-10 (고급 설정 패널 — RSI 하한/골든크로스 창/편입임계 자유 입력,
  범위 강제 30~70/1~20/50~95). ⚠ 이번에 남긴 갭: 골든크로스 창이 임의값(예:7,13)이 되면
  recommend.js의 GOLDEN_CROSS_FIELDS 이산 매핑({3,5,6,10,20}만 지원)이 깨진다 — US-10은
  반드시 (a) deriveTickerData에 macdLine/signalLine 원본 배열을 추가로 저장하고
  recommend.js가 goldenCrossWithin()을 그 배열로 직접 즉석 계산하도록 일반화하거나,
  (b) 다른 방식으로 임의 창을 지원해야 함. App.jsx의 activeConfig 계산에도
  preset==='custom'일 때 customParams를 사용하는 분기를 추가해야 함.

## Iteration 10 — US-10. 고급 설정 패널 (자유 파라미터) + 골든크로스 창 일반화
- 완료 스토리: US-10
- 변경 파일:
  - src/lib/deriveTickerData.js: indicators.macdLineSeries/signalLineSeries(원본 MACD·
    시그널 전체 배열) 추가 — 임의 골든크로스 창 계산의 데이터 소스.
  - src/lib/recommend.js: goldenCrossPass(td, window) 헬퍼 신설 — GOLDEN_CROSS_FIELDS
    이산 매핑(3/5/6/10/20)에 먼저 시도하고, 없으면 macdLineSeries/signalLineSeries로
    goldenCrossWithin()을 즉석 계산하는 경로로 자동 폴백. 기존 프리셋·테스트 픽스처는
    이산 매핑을 그대로 타므로 무영향(회귀 없음, recommend.test.js 22개 그대로 통과).
  - src/components/AdvancedSettingsPanel.jsx(신설): 기본 접힘 패널, RSI 하한/골든크로스
    창/고득점 편입 임계 3개 숫자 입력(범위 강제 clamp), "기본형 X → 현재 Y" 변경 표시,
    "기본형으로 초기화" 버튼. 상태를 소유하지 않고 onParamChange/onResetToDefault만 호출.
  - src/screens/Recommend.jsx: AdvancedSettingsPanel을 프리셋 세그먼트 아래 삽입
    (customParams가 있을 때만 — 구형 테스트 호출부 하위 호환).
  - src/App.jsx: changePreset(key)가 preset 전환 시 customParams도 그 프리셋 값으로
    덮어씀. changeCustomParam(key,value)이 파라미터 변경 시 preset='custom'으로 전환.
    activeConfig 계산에 preset==='custom' 분기 추가(customParams → config 변환,
    완화 창 = 골든크로스 창 × 2, "창 2배" 규칙 그대로 임의값에 적용).
  - 신규 테스트: AdvancedSettingsPanel.test.jsx 7개(기본 접힘/범위 클램프 3종/초기화
    버튼/변경 힌트), recommend.test.js에 임의 창(7) 계산·폴백 3개, App.test.jsx에
    "조정→사용자 설정→프리셋 클릭→덮어씀" + "초기화 버튼" 실데이터 통합 테스트 2개.
- 테스트 수 변화: 167 → 179 (+12), 전체 통과. npm run build 통과(187.75kB→190.19kB).
- 운영자 액션 필요: 없음
- ⚠ 남은 갭 없음 — US-8/US-9에서 이월된 "임의 창 일반화"와 "custom 분기 배선" 모두
  이번 스토리에서 해소함.
- 다음 스토리 메모: US-11 (리서치 요청 목록, v6 연계 절충안 A — 마지막 스토리).
  ResearchRequestList.jsx 신설 + 화면1/2/3 종목 카드에 토글 버튼. researchRequests는
  이미 US-6에서 스키마·마이그레이션·유니버스 필터링까지 구현돼 있으므로 이번엔 순수
  UI(토글/목록 패널/복사)만 추가하면 됨. 클립보드 API 실패 폴백(선택 가능한 텍스트 노출)
  필요. 실행 트리거·API 호출 코드는 추가하지 않음(Out of Scope).

## Iteration 11 — US-11. 리서치 요청 목록 (v6 연계, 절충안 A) — v7 마지막 스토리
- 완료 스토리: US-11 (prd-v7-filters-presets.md의 US-1~US-11 전부 완료)
- 변경 파일:
  - src/components/ResearchRequestToggle.jsx(신설): 종목 카드/행에 붙는 담기/해제 토글.
    stopPropagation으로 상위 label(있는 경우) 클릭 전파 차단.
  - src/components/ResearchRequestList.jsx(신설): 공통 접이식 패널(기본 접힘), 개수 배지,
    개별 제거(✕), 전체 비우기, "목록 복사"(navigator.clipboard.writeText, 쉼표+공백
    구분) + 실패 시 선택 가능한 텍스트 입력 폴백. 리서치를 실행하지 않음(Out of Scope
    그대로 준수 — API 호출 코드 없음).
  - src/screens/HomeSearch.jsx / Recommend.jsx / Simulation.jsx: 각 종목 행/카드에
    ResearchRequestToggle 삽입(onToggleResearchRequest prop 있을 때만 렌더 — 구형
    테스트 호출부 하위 호환). Recommend.jsx는 US-6의 ResearchSection과 동일하게 label
    바깥(형제)에 배치해 체크박스 오작동 방지.
  - src/screens/Portfolio.jsx: **무수정** (PRD 요구사항 그대로 — 화면4는 변경 없음).
  - src/App.jsx: toggleResearchRequest/removeResearchRequest/clearResearchRequests 핸들러
    추가. `<ResearchRequestList>`를 NavTabs 바로 아래, 화면 분기 바깥에 전역 배치 —
    "공통" 요소이므로 4개 화면 어디서나 접근 가능(화면4 포함, Portfolio.jsx 자체는 무수정).
  - 신규 테스트: ResearchRequestToggle.test.jsx 4개, ResearchRequestList.test.jsx 8개
    (클립보드 성공/폴백 모킹 포함), 화면별 토글 렌더링 테스트 3화면×2개, App.test.jsx에
    토글→전역 목록 반영→새로고침 후 유지 실데이터 통합 테스트 1개.
- 테스트 수 변화: 179 → 199 (+20), 전체 통과. npm run build 통과(190.19kB→193.83kB).
- 운영자 액션 필요: 없음
- **PRD_Nasdaq7 v7 — US-1~US-11 전부 완료.** §8 성공 기준 대부분은 코드 레벨에서
  충족(회귀·클램프·마이그레이션·복사 등 전부 테스트로 검증). 유일하게 코드로 검증 불가한
  항목: "12개월 데이터 재수집 후 기존 전 기능이 회귀 없이 동작한다" — US-1에서 스크립트만
  전환했고 실제 재수집(python 환경 필요)은 운영자 수동 작업으로 남아 있음(US-1 기록과 동일).
  재수집 후에는 sixMonth=126거래일, week52 필터가 실제로 값을 갖게 되는지(현재는 123일
  데이터라 전부 null) 별도 확인 필요.


# v8 Ralph 루프 진행 기록 (prd-v8-dual-mode_1.md)

## Iteration 1 — US-1. 데이터 수집 2년 확대
- 완료 스토리: US-1
- 변경 파일:
  - scripts/collect_data.py: PERIOD "13mo"→"2y" (약 504거래일). MIN_TRADING_DAYS(200)는
    v7과 동일하게 유지(PRD 명시 요구사항 — 미너비니용 252거래일 미만 판정은 수집 단계가
    아니라 클라이언트의 hasFullYearData()가 개별 처리하도록 이번 스토리에서는 손대지
    않음). main() 말미에 파일 크기(MB) 로그 + 10MB 초과 시 [WARN] 로그 추가.
  - src/lib/__fixtures__/nasdaq100.2y.sample.json(신설): 결정적(비랜덤) 합성 데이터
    생성기로 만든 6종목×504거래일 픽스처. 종목별로 서로 다른 패턴을 의도적으로 구성함
    (MMVI1=강한 상승+최근 변동성수축+막판 돌파, MMVI2=지속 하락, MMVI3=변동성 큰 완만한
    상승, MMVI4=횡보, MMVI5=꾸준한 완만 상승, MMVI6=장기 상승+VCP형 수축) — 이후 US-3~5의
    미너비니/VCP 지표 테스트가 이 다양성을 활용할 수 있도록. buildDataset()에 실제로
    통과시켜 스키마 정합성 확인(전 종목 dataSufficient:true).
- 테스트 수 변화: 없음(199 유지, Python 스크립트는 vitest 대상 아님). npm run build 통과.
- 운영자 액션 필요: collect_data.py 재실행(2년치)은 이 루프가 하지 않음(PRD 절대 원칙) —
  별도 세션에서 python 환경으로 실행 필요. 참고: v7에서 "13mo"로 이미 52주 필터 문제를
  해결해 둔 상태이므로, 2년 재수집 전까지는 기존 13개월 실데이터가 계속 서비스된다
  (회귀 없음 — 미너비니 관련 신규 기능은 fundamentals.json과 마찬가지로 데이터 없이도
  동작해야 하는 graceful degradation 대상이 될 예정).
- 다음 스토리 메모: US-2 (fundamentals.json 수집 + 스키마 검증기. collect_data.py에
  펀더멘털 수집 로직 추가 + src/lib/fundamentalsSchema.js + 유효/무효 픽스처. v6의
  atomicWriteResearch 원자적 쓰기 패턴 재사용. 실행은 하지 않음 — 스크립트·검증기·
  픽스처까지만).

## Iteration 2 — US-2. fundamentals.json 수집 + 스키마 검증기
- 완료 스토리: US-2
- 변경 파일:
  - scripts/collect_data.py: fetch_fundamentals()(yfinance 분기재무+info로 F1~F5 파생값
    계산, EPS는 순이익/발행주식수 근사) + collect_fundamentals()(원자적 쓰기: .json.tmp
    작성 후 os.replace) 추가, main() 말미에서 가격 수집과 같은 실행으로 호출.
    `python -m py_compile`로 구문 검사만 수행(PRD 절대 원칙 — 실행은 하지 않음).
  - src/lib/fundamentalsSchema.js(신설): validateFundamentals() + atomicWriteFundamentals()
    — research.json(v6)과 동일한 tmp-then-rename 패턴. missing[] 값은 F1~F5 enum으로 검증.
  - src/lib/__fixtures__/fundamentals.{valid,invalid}.json(신설): invalid는 schemaVersion
    오류+roe 타입 오류+missing 잘못된 코드 3가지를 한 픽스처에 동시 포함(research.invalid.json
    패턴 재사용).
  - src/lib/fundamentalsSchema.test.js(신설): 12개 테스트(픽스처 2 + 개별 실패 케이스 6 +
    원자적 쓰기 4).
- 설계 결정: PRD가 명시한 파일은 fundamentalsSchema.js 하나뿐이라(v6 research.json처럼
  별도 CLI 스크립트가 명시되지 않음) atomicWriteFundamentals를 이 파일 안에 포함시켰다
  — 실제 프로덕션 쓰기는 Python이 담당하고, JS 쪽 atomic-write는 승인 기준이 요구하는
  vitest 검증 가능한 참조 구현 역할.
- 테스트 수 변화: 199 → 211 (+12), 전체 통과. npm run build 통과(번들 크기 불변 —
  아직 App에서 import 안 됨, 정상).
- 운영자 액션 필요: 없음 이번 스토리에서는 (fundamentals 실데이터 수집은 US-1과 마찬가지로
  루프가 실행하지 않음 — 2년 가격 재수집과 함께 나중에 운영자가 한 번에 실행).
- 다음 스토리 메모: US-3 (공유 지표 계층 + constants/v8.js. sma() 일반화(50/150/200 지원,
  기존 SMA20 동작 불변 확인 필수), rsRawScore/rsPercentile/volatilityContraction/
  volumeDryUp/pivotProximity 신규, hasFullYearData() 헬퍼. src/lib/__fixtures__/
  nasdaq100.2y.sample.json(US-1에서 만든 6종목 다양 패턴)을 지표 테스트에 활용).

## Iteration 3 — US-3. 공유 지표 계층 + 상수 모듈
- 완료 스토리: US-3
- 변경 파일:
  - src/lib/indicators.js: hasFullYearData/rsRawScore/rsPercentile/volatilityContraction/
    volumeDryUp/pivotProximity 6개 함수 추가(기존 함수 전부 무수정). sma()는 이미
    임의 period를 지원하므로 SMA50/150/200용 별도 함수를 만들지 않고 그대로 재사용
    (US-3의 "일반화" 요구사항은 확인만으로 충족됨 — 코드 변경 없음).
  - src/lib/constants/v8.js(신설, 새 디렉터리): TREND_TEMPLATE/VCP_SCORE/
    FUNDAMENTAL_THRESHOLDS 전부 "v9 백테스트로 조정 예정인 설계값" 주석과 함께 정의.
    src/lib/constants.js(v7, 파일)와 src/lib/constants/(v8, 디렉터리)가 이름 충돌 없이
    공존함을 확인.
  - src/lib/__fixtures__/nasdaq100.2y.sample.json(수정): MMVI1의 "막판 돌파"가
    volatilityContraction의 recent10 측정 구간을 오염시켜(9.98배로 오히려 확대) US-1에서
    의도한 "변동성 수축" 테스트가 깨지는 것을 발견 — 돌파 이벤트를 제거하고 순수 수축
    패턴만 남기도록 픽스처 재생성(생성기 스크립트도 스크래치패드에서 함께 수정).
  - src/lib/indicators.test.js: makeBar() 헬퍼가 volume 오버라이드를 무시하던 버그를
    발견해 수정(기존 테스트는 영향 없음 — 다들 기본값 1,000,000 사용). 신규 지표 6개에
    대해 15개 테스트 추가(경계: 분모 0 2건, 데이터 부족 5건, 음수 드라이업 1건 포함).
- 설계 결정: rsRawScore/volatilityContraction/volumeDryUp/pivotProximity의 윈도우 크기
  (63/126/252, 10/40, 5/50)는 PRD가 constants/v8.js 목록에 명시하지 않은 "알고리즘
  구조 상수"로 판단해(v7의 SIM_WINDOW=63 같은 기존 로컬 상수 패턴과 동일) 함수 내부에
  JSDoc 설명과 함께 남겨두고 constants/v8.js로 옮기지 않음 — v9 백테스트로 조정될
  대상은 임계값·배점(트렌드 템플릿/VCP/펀더멘털)뿐이므로 이 구분이 PRD 의도와 부합.
- 테스트 수 변화: 211 → 226 (+15, indicators 28→43), 전체 통과. npm run build 통과
  (번들 크기 불변 — 아직 App에서 import 안 됨).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-4 (미너비니 1단계 트렌드 템플릿, src/lib/minervini.js 신설).
  T1~T8 판정 시 constants/v8.js의 TREND_TEMPLATE 상수 사용, hasFullYearData()로
  데이터 부족 종목 사전 제외, week52HighLow()는 v7 것 재사용(재구현 금지). 완화 폴백은
  TREND_TEMPLATE_RELAXED_MIN_CONDITIONS(7)/TREND_TEMPLATE_MIN_RESULTS(5) 사용.
  nasdaq100.2y.sample.json의 MMVI1(상승+수축)/MMVI2(하락)/MMVI4(횡보) 등으로 템플릿
  통과·탈락 케이스 검증 가능.

## Iteration 4 — US-4. 미너비니 모드 — 1단계 트렌드 템플릿
- 완료 스토리: US-4
- 변경 파일:
  - src/lib/minervini.js(신설): evaluateTrendTemplate(series, rsPercentileValue)(T1~T8
    판정 + passedCount/allPassed/missingConditions/insufficientData) + runMinerviniStage1
    (tickers[{ticker,name,sector,series}]) — 유니버스 rsRawScore→rsPercentile 계산 후
    8/8 시도, MIN_RESULTS(5) 미달 시 7/8로 완화(2단계뿐, 추세추종처럼 3단계 아님),
    그래도 부족하면 있는 만큼만 + insufficientSignal. 데이터 252일 미만 종목은
    excludedForInsufficientData로 분리 반환. week52HighLow는 v7 것 재사용(재구현 안 함).
  - src/lib/minervini.test.js(신설): 14개 테스트. T6/T7 경계값은 week52HighLow가
    high/low만 참조하고 close는 안 본다는 점을 이용해 "한 바의 high/low만 인위적으로
    벌려 window의 min/max를 고정하고, 마지막 바의 close만 정확한 경계값으로 맞추는"
    기법으로 정밀 테스트. T3는 단조 상승/하락 시리즈로 SMA200의 22일 전 대비 방향을
    구조적으로 검증. 완화 폴백 테스트는 "상승 추세 유지 + 한 바의 high만 인위적으로
    키워 52주 고점만 왜곡"시켜 T7 하나만 표적으로 실패시키는 기법 사용 — 처음에
    조작 인덱스를 252일 관측창(마지막 252거래일) 밖에 둬서 효과가 없었던 실수를
    발견해 인덱스를 창 안으로 수정함(디버깅 기록).
- 테스트 수 변화: 226 → 240 (+14), 전체 통과. npm run build 통과(번들 크기 불변 —
  아직 App에서 import 안 됨).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-5 (미너비니 2단계 VCP 스코어링, minervini.js 확장). RS백분위40+
  변동성수축25+거래량드라이업15+피벗근접20 선형·클램프, constants/v8.js의 VCP_SCORE
  상수 사용. 결과 객체를 recommend.js 결과와 동형(ticker,score,reasons,signalPassed,
  relaxationApplied,templateChecks[])으로 맞춰 US-6 컨센서스 랭킹이 두 모드를 동일하게
  다룰 수 있게 할 것. 이유 문자열은 0점 요소 생략.

## Iteration 5 — US-5. 미너비니 모드 — 2단계 VCP 근사 스코어링
- 완료 스토리: US-5
- 변경 파일:
  - src/lib/minervini.js 확장: evaluateVcpScore(series, rsPercentileValue) — RS 40 +
    변동성수축 25 + 드라이업 15 + 피벗근접 20, 전부 constants/v8.js의 VCP_SCORE 사용.
    PRD 원문 수식을 그대로 옮기면 변동성수축 항이 상한(25)을 초과할 수 있는 결함을
    발견해(수축비 0.3이면 35점이 나옴) clamp01()로 보정 — "0.5 이하에서 만점"이라는
    PRD의 서술을 문자 그대로 만족시키도록 수정(원문 수식의 오탈자로 판단). 이유 문자열은
    "Stage 2 추세" 고정 + 0점이 아닌 요소만 조건부 추가(RS 상위 n% / 변동성 수축 중 /
    피벗 −n%) — PRD 예시 형식 그대로, 드라이업은 이유 문구에 포함하지 않음(PRD 예시에
    없음). runMinerviniRecommend(tickers) — 1+2단계 통합 파이프라인, 출력 필드를
    recommend.js와 동형(ticker/name/sector/score/reasons/signalPassed/
    relaxationApplied) + 배열 형태의 templateChecks(US-10의 8칸 체크리스트 렌더링을
    염두에 두고 객체가 아닌 [{code,passed}] 배열로 설계).
  - src/lib/minervini.test.js 확장: 14개 테스트. 변동성수축 배점은 "정확히 0.5/1.0"이
    아니라 "0.5보다 충분히 작음/1.0 이상"을 지수적 등락 종가 시리즈로 구성해 검증(정확한
    분수 경계를 손으로 맞추기보다 클램핑 동작 자체를 검증하는 실용적 접근 — RS/드라이업/
    피벗은 폐형 수식이라 정확한 경계값으로 손계산 검증).
- 테스트 수 변화: 240 → 254 (+14), 전체 통과. npm run build 통과(번들 크기 불변).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-6 (컨센서스 랭킹, src/lib/consensus.js 신설). 입력은 기존
  recommend.js(수정 금지) 결과 + runMinerviniRecommend() 결과 — 원점수 합산 금지,
  각 모드 1단계 통과 집합 내 백분위 순위 평균으로 정렬. ★★(양쪽 통과)/★(한쪽 통과)
  등급, ★★가 항상 ★ 위. "평균의 함정 방지" 테스트(한쪽90/한쪽40 종목보다 양쪽75/75가
  위) 필수 — indicators.js의 percentileRank류 로직 재사용 가능한지 검토(rsPercentile과
  동일한 "값 이하 개수/전체" 공식).

## Iteration 6 — US-6. 컨센서스 랭킹
- 완료 스토리: US-6
- 변경 파일:
  - src/lib/consensus.js(신설): buildConsensusRanking(trendResult, minerviniResult).
    각 모드 결과의 list 내부 score 배열을 모집단으로 자기 자신의 백분위를 계산(indicators.js
    rsPercentile과 동일한 "값 이하 개수/전체" 공식, 이번에도 재사용하지 않고 로컬 구현 —
    filters.js/indicators.js 각각 독립 구현해 온 기존 관례와 동일). 정렬은 등급(★★ 항상
    ★ 위) 1차, consensusPercentile(백분위 단순 평균, 원점수 합산 아님) 내림차순 2차.
    trendResult/minerviniResult가 null이거나 list가 비어도 안전하게 동작(옵셔널 체이닝).
  - src/lib/consensus.test.js(신설): 6개 테스트. "평균의 함정" 테스트는 population을
    정확히 1..100 정수로 구성해 percentileOf(v)=v가 항상 성립하게 만드는 기법으로
    손 계산 없이 정확한 기대값을 얻음(90/40 조합 vs 75/75 조합 비교, 100/★ vs 10★★
    비교 등).
- 테스트 수 변화: 254 → 260 (+6), 전체 통과. npm run build 통과(번들 크기 불변 — 아직
  App에서 import 안 됨).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-7 (펀더멘털 허들 판정 + 로더, src/lib/fundamentals.js +
  fundamentalsLoader.js 신설). US-2의 fundamentalsSchema.js/atomicWriteFundamentals는
  검증·쓰기 전용이었으므로, 이번 스토리에서 실제 로더(fetch, v6 researchLoader 패턴)와
  Pass/Partial/Fail/insufficientFundamentals 4분기 판정 로직을 새로 만들어야 함.
  F1·F3·F5 핵심 3종은 constants/v8.js의 FUNDAMENTAL_THRESHOLDS 사용, F2·F4는 판정에
  미반영(참고 배지 값만 통과).

## Iteration 7 — US-7. 펀더멘털 허들 판정 + 로더
- 완료 스토리: US-7
- 변경 파일:
  - src/lib/fundamentalsLoader.js(신설): loadFundamentals()(researchLoader.js와 동일
    패턴 — 404/파싱실패/스키마불일치 전부 null, 에러 UI 없음) + buildFundamentalsMap()
    (유니버스 밖 티커 조용히 제외).
  - src/lib/fundamentals.js(신설): evaluateFundamentalHurdle(item) — item이 null이면
    (fundamentals.json에 해당 티커 없음) null 반환, 상위에서 허들 단계 자체를 생략하도록
    구분. 핵심 F1(EPS≥20%)/F3(매출≥20%)/F5(ROE≥17%)는 constants/v8.js의
    FUNDAMENTAL_THRESHOLDS 사용, missing[]에 있거나 값이 null인 기준은 판정불가(null)로
    분모 제외. 판정불가 2개 이상이면 fail이 아니라 insufficientFundamentals로 분리.
    F2(epsAccelerating)/F4(marginImproving)는 판정에 미반영, missing 처리된 참고 배지
    값만 통과. 근거 문자열은 판정불가/충족/미충족 3갈래로 생성(예:
    "EPS +31% ✓" / "매출 +12% ✗" / "ROE 판정불가") — PRD 예시의 EPS·매출은 부호 있는
    퍼센트, ROE는 부호 없는 정수 퍼센트로 포맷을 구분.
  - src/lib/fundamentals.test.js(신설): 16개 테스트(pass/fail/partial×2/
    insufficientFundamentals×3/F2·F4 미반영×2/근거 문자열 4종 + null 입력 + 경계값 2건).
  - src/lib/fundamentalsLoader.test.js(신설): 7개 테스트(researchLoader.test.js와
    동일 구조 — 성공/404/스키마불일치/throw + map 필터링).
- 설계 결정: US-2의 fundamentalsSchema.js는 검증/원자적쓰기 전용이었고 이번 스토리가
  실제 소비측(로더+판정) 최초 구현. App.jsx/화면 배선은 아직 하지 않음(PRD상 US-11의
  범위) — 이번 스토리는 순수 함수+로더까지만, 화면3 통합 여부 확인 기준(승인 기준 2)은
  "아직 어디서도 import되지 않아 회귀 자체가 발생할 수 없음"으로 충족.
- 테스트 수 변화: 260 → 283 (+23, fundamentals 0→16, fundamentalsLoader 0→7), 전체
  통과. npm run build 통과(193.83kB, 번들 크기 불변 — 아직 App에서 import 안 됨).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-8 (research.json 스키마 v2 — riskFlags). researchSchema.js에
  riskFlags:[{type,description}] 필드 추가(type enum: earnings_imminent/litigation/
  regulatory/guidance_cut/other), schemaVersion:2. v1 파일 하위 호환 필수 —
  researchLoader.js의 buildResearchMap이나 검증 로직에서 v1 로드 시 riskFlags를 빈
  배열로 간주하도록 처리(기존 v1 픽스처 테스트 유지). .claude/commands/research.md에
  리스크 플래그 작성 가이드 1개 절 추가.

## Iteration 8 — US-8. research.json 스키마 v2 (riskFlags)
- 완료 스토리: US-8
- 변경 파일:
  - src/lib/researchSchema.js: SUPPORTED_SCHEMA_VERSIONS=[1,2]로 확장(기존 SCHEMA_VERSION=1
    상수는 그대로 유지, export도 하위 호환). validateItem에 schemaVersion 인자 추가 —
    schemaVersion===2일 때만 item.riskFlags(배열, 각 원소 {type,description}, type은
    RISK_FLAG_TYPES enum) 검증. v1 문서는 riskFlags 필드 존재 여부와 무관하게 검증하지
    않음(진짜 하위 호환 — v1인데 riskFlags가 있어도/없어도 통과).
  - src/lib/researchLoader.js: buildResearchMap이 각 item에 riskFlags: item.riskFlags ?? []
    스프레드로 정규화 — v1 로드 시 화면 쪽에서는 riskFlags가 항상 빈 배열로 보임(추가
    분기 없이 안전하게 순회 가능).
  - .claude/commands/research.md: "리스크 플래그(riskFlags) 작성 규칙" 신규 절 추가
    (기존 규칙 9개 뒤, Out of Scope 앞) — type 5종 설명, 규칙1(권유 금지)·규칙7(근거
    없으면 기록 안 함) 재확인 형태로 기존 규칙과 모순 없이 연결. ⑤단계 문구에 "리스크
    플래그를 기록할 경우 버전 2" 안내 추가.
  - 기존 테스트 수정: researchSchema.test.js의 "rejects a wrong schemaVersion" 테스트가
    schemaVersion:2를 무효값으로 가정하고 있었으므로(이제 유효값) schemaVersion:3으로
    교체해 "지원하지 않는 버전" 의미를 보존.
  - 신규 테스트: researchSchema.test.js에 v2 riskFlags 6개(v1 하위호환/유효/빈배열/
    잘못된type/description누락/riskFlags필드자체누락), researchLoader.test.js에
    정규화 2개(v1→빈배열, v2→그대로 보존).
- 설계 결정: PRD가 "v1 파일 로드 시 riskFlags를 빈 배열로 간주 (researchLoader 수정)"라고
  명시했으므로 정규화 책임을 researchSchema.js(검증)가 아니라 researchLoader.js(소비
  직전 변환)에 두었다 — validateResearch는 순수 검증만 하고 데이터를 변형하지 않는
  기존 원칙(v1부터 유지)을 그대로 지킴.
- 테스트 수 변화: 283 → 291 (+8, researchSchema 10→16, researchLoader 8→10), 전체
  통과. npm run build 통과(193.83kB→194.40kB, 트리셰이킹 안 되는 순수 함수 코드 증가분).
- 운영자 액션 필요: 없음 (다음 /research 실행 시 리스크 플래그를 기록하려면
  schemaVersion:2로 저장하면 됨 — 기존처럼 리스크 플래그 없이 진행해도 무방, 강제 아님)
- 다음 스토리 메모: US-9 (localStorage 스키마 v3). 기존 persistence.js의 SCHEMA_VERSION
  2→3, MIGRATABLE_FROM_VERSION을 2로 갱신(v7의 1→2 마이그레이션 패턴 재사용). 신규 필드
  recommendMode("consensus"|"trend"|"minervini", 기본 consensus)/hideRiskFlagged(기본
  false)/showFundamentalFail(기본 false). 저장된 recommendMode가 enum 밖이면 consensus로
  리셋(customParams 필드 단위 검증과 동일한 방어적 패턴). 기존 필드(선택종목/프리셋/
  customParams/researchRequests) 전부 무손실 보존 확인 필수.

## Iteration 9 — US-9. localStorage 스키마 v3
- 완료 스토리: US-9
- 변경 파일:
  - src/lib/persistence.js: SCHEMA_VERSION 2→3. MIGRATABLE_FROM_VERSION(단일값)을
    MIGRATABLE_FROM_VERSIONS=[1,2] 배열로 일반화 — v1 레코드도 v2 전용 필드 유무와
    무관하게 동일한 병합 로직으로 안전하게 v3까지 한 번에 마이그레이션됨을 확인했으므로
    별도의 v1→v2→v3 단계 분리 없이 하나의 로드 경로로 처리. DEFAULT_UI_STATE에
    recommendMode('consensus')/hideRiskFlagged(false)/showFundamentalFail(false) 추가.
    RECOMMEND_MODE_VALUES enum 검증(밖이면 consensus로 리셋), 나머지 2개 신규 필드는
    typeof boolean 아니면 기본값(false)으로 방어.
  - src/lib/persistence.test.js: 기존 v1→v2 테스트의 `loaded.schemaVersion` 기대값을
    2→3으로 갱신(스키마가 3으로 올라갔으므로), 같은 테스트에 v3 신규 필드 기본값 검증
    3개 추가. 신규 describe 블록(v2→v3 마이그레이션) 4개 테스트: 기존 필드(선택종목/
    preset/customParams/researchRequests) 무손실 보존, 잘못된 recommendMode 리셋,
    3개 enum 값 각각 왕복 확인, hideRiskFlagged/showFundamentalFail 토글 왕복.
- 설계 결정: PRD가 "v1→v2 패턴 재사용"이라 명시했지만 기존 코드가 단일 정수
  MIGRATABLE_FROM_VERSION이라 v1을 v3로 바로 매핑하려면 배열화가 필요했음 — 정확히
  "v2에서 마이그레이션"만 지원하면 v1 레코드가 로드 시 완전 리셋되는 회귀가 생기므로
  [1,2] 배열로 확장. 이 결정이 기존 v1→v2 테스트(스키마 2로 착지)의 스펙과 어긋나지
  않도록, 해당 테스트를 스키마 3으로 착지하는 것으로 최신화(자연스러운 결과 — v1
  레코드는 이제 v3까지 한 번에 마이그레이션됨).
- 테스트 수 변화: 291 → 295 (+4, persistence 9→13), 전체 통과. npm run build 통과
  (194.40kB→194.78kB).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-10 (화면2 — 모드 세그먼트 + 통합 뷰). Recommend.jsx에 v7 프리셋
  세그먼트 패턴을 재사용해 모드 3단 세그먼트(통합/추세추종/미너비니) 추가, App.jsx가
  uiState.recommendMode에 따라 recommend()/runMinerviniRecommend()/
  buildConsensusRanking() 중 무엇을 호출할지 분기해야 함(US-6 consensus.js가 이미
  이 3개를 조합하는 지점). "추세추종" 모드에서만 프리셋/고급설정 패널 노출(기존 v7 UI
  그대로). 통합 뷰 카드에 ★★/★ 등급 + 양 모드 점수 + 통합 백분위 문구 렌더링.

## Iteration 10 — US-10. 화면2 — 모드 세그먼트 + 통합 뷰
- 완료 스토리: US-10
- 변경 파일:
  - src/lib/deriveTickerData.js: dataSufficient:true 반환 객체에 원본 series(raw bar
    배열) 필드를 추가로 보존(기존 indicators/chart 필드 전부 무수정, 추가 전용) —
    minervini.js(runMinerviniStage1/runMinerviniRecommend)가 rsRawScore/hasFullYearData
    등을 계산하려면 파생 지표가 아니라 원본 series가 필요하기 때문. applyFilters()는
    .filter()만 하므로 새 필드는 filteredTickers까지 그대로 통과함을 확인.
  - src/App.jsx: runMinerviniRecommend(filteredTickers)와 buildConsensusRanking(
    recommendation, minerviniResult)를 useMemo로 계산해 Recommend에 추가 전달.
    changeRecommendMode(mode) 핸들러 신설(uiState.recommendMode 갱신, US-9에서 이미
    스키마·마이그레이션 준비됨). 미너비니는 원전 기준 고정이므로 activeConfig(프리셋)를
    받지 않음 — filteredTickers만 입력.
  - src/screens/Recommend.jsx 전면 재구성: 상단 3단 모드 세그먼트(ModeSegment, 기본
    'trend' — App.jsx가 항상 uiState.recommendMode를 명시적으로 넘기므로 실사용 기본은
    "통합"이지만, prop 자체의 기본값은 'trend'로 둬 recommendMode를 전달하지 않는
    기존 Recommend.test.jsx 호출부가 전부 그대로 통과하게 함. "config 없이 호출 시
    기존 동작 유지"라는 v7 US-8 관례를 그대로 재사용). 3개 서브뷰로 분리:
    TrendModeView(기존 JSX를 그대로 이동, 로직 무변경), MinerviniModeView(VCP 점수순
    카드 + TrendTemplateChecklist 8칸 T1~T8 체크리스트 + 완화 배너
    "조건 완화 적용됨(7/8)"(TREND_TEMPLATE_RELAXED_MIN_CONDITIONS/TREND_TEMPLATE.
    CONDITION_COUNT 상수 사용) + insufficientSignal 시 "현금 보유가 원칙입니다" 문구),
    ConsensusModeView(★★/★ 등급 배지 + "추세추종 N점 · 미너비니 M점 · 통합 상위 P%"
    형식 카드). 신규 SelectAndResearch 헬퍼로 체크박스<label>과 리서치 요청 토글/
    리서치 상세를 분리 배치(v7 US-11 "체크박스 오작동 방지" 패턴 재사용 — 처음에
    ResearchSection을 children으로 잘못 넣어 label 안에 들어가는 실수를 발견해
    researchSection 별도 prop으로 수정). 미너비니·통합 뷰 하단 공통
    DesignValueNotice("배점·기준값은 v9 백테스트로 조정 예정인 설계값입니다").
  - 기존 테스트 수정: App.test.jsx의 프리셋/고급설정 테스트 3개가 이제 기본 모드가
    "통합"이라 프리셋 세그먼트에 도달할 수 없으므로, 각 테스트 시작부에 "추세추종" 버튼
    클릭을 추가(동작 자체는 무수정, 진입 경로만 추가).
  - 신규 테스트: deriveTickerData.test.js에 series passthrough 2개, Recommend.test.jsx에
    모드 세그먼트/미너비니 뷰/통합 뷰 11개, App.test.jsx에 실데이터 기반 3모드 전환 +
    localStorage 영속 통합 테스트 2개.
- 설계 결정: Recommend.jsx 컴포넌트 자체의 recommendMode 기본값을 'trend'로 둔 이유는
  "추세추종 모드에서 기존 화면2 테스트 전부 통과"(승인 기준 2)를 기존 테스트 파일을
  건드리지 않고 만족시키기 위함 — App.jsx는 항상 명시적으로 uiState.recommendMode
  (기본 'consensus', US-9)를 전달하므로 실사용 동작과 무관.
- 테스트 수 변화: 297 → 310 (+13, deriveTickerData 6→8, Recommend 10→21, App 10→12),
  전체 통과. npm run build 통과(194.78kB→206.21kB — minervini/consensus 로직과 신규
  뷰가 이제 App에서 실제로 import되어 트리셰이킹 대상에서 빠짐, 예상된 증가).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-11 (화면2 — 펀더멘털 배지 + Fail 하단 섹션). fundamentals.js/
  fundamentalsLoader.js(US-7)를 처음으로 App.jsx에 배선 — loadFundamentals() 호출 +
  buildFundamentalsMap()으로 티커별 맵 구성(researchMap과 동일 패턴). 카드에
  Pass/Partial/insufficientFundamentals 배지, Fail은 하단 접이식 섹션으로 분리(숨기지
  않음). fundamentals.json 부재 시 배지·섹션 전부 미표시 + US-10 상태와 시각적으로
  동일해야 함(graceful degradation 테스트 필수) — 어느 모드(통합/추세추종/미너비니)
  카드에도 공통 적용되는 것으로 보이므로 SelectAndResearch 근처에 공통 배지 삽입 지점을
  두는 편이 3개 뷰에 중복 없이 적용하기 좋을 것.

## Iteration 11 — US-11. 화면2 — 펀더멘털 배지 + Fail 하단 섹션
- 완료 스토리: US-11
- 변경 파일:
  - src/App.jsx: loadFundamentals()/buildFundamentalsMap() 호출을 research.json 로드와
    같은 useEffect에 순차 추가(둘 다 선택적 스냅샷 — 하나가 없어도 앱은 정상 동작),
    fundamentalsMap state 신설, Recommend에 prop으로 전달.
  - src/components/FundamentalBadge.jsx(신설): evaluateFundamentalHurdle() 결과 1건을
    받아 Pass/Partial/판정불가 배지 + 펼치면 근거 3종(F1/F3/F5) + F2/F4 참고 배지 표시.
    evaluation이 null이면(fundamentals.json 부재/해당 티커 없음) 아무것도 렌더링하지
    않음 — verdict==='fail'은 이 컴포넌트에 도달하지 않는다(카드 자체가 하단 섹션으로
    분리되므로).
  - src/components/FundamentalFailSection.jsx(신설): Fail 종목을 숨기지 않고 사유와
    함께 보여주는 공통 접이식 섹션(기본 접힘, ResearchRequestList.jsx 패턴 재사용).
  - src/screens/Recommend.jsx: splitByFundamentalVerdict(list, fundamentalsMap) 헬퍼
    신설 — evaluateFundamentalHurdle(fundamentalsMap?.get(ticker))이 'fail'이면 failed[]로,
    아니면(pass/partial/insufficientFundamentals/null) visible[]로 분리하고 각 항목에
    fundamentalEvaluation을 붙임. fundamentalsMap이 없거나 해당 티커가 없으면 evaluation이
    자연히 null이 되어 별도 분기 없이도 US-10과 동일하게 렌더링됨(설계 그대로 검증됨).
    TrendModeView/MinerviniModeView/ConsensusModeView 3개 뷰 전부에서 원본 list 대신
    visible을 렌더링하고 하단에 FundamentalFailSection(failed) 추가. SelectAndResearch에
    fundamentalEvaluation prop을 추가해 FundamentalBadge를 <label> 바깥(체크박스 오작동
    방지 위치)에 렌더링.
  - 신규 테스트: FundamentalBadge.test.jsx 7개, FundamentalFailSection.test.jsx 5개,
    Recommend.test.jsx에 4개 판정 상태별 렌더링 + Fail 분리 배치 + graceful degradation
    통합 테스트 3개.
- 버그 발견 및 수정(빌드 단계): Recommend.jsx가 fundamentals.js를 import하면서
  fundamentalsLoader.js→fundamentalsSchema.js 체인이 처음으로 App.jsx(브라우저 번들)에
  실제로 포함되게 됐는데, US-2에서 fundamentalsSchema.js에 넣어둔 atomicWriteFundamentals가
  node:fs를 import하고 있어 `npm run build`가 즉시 실패함("writeFileSync is not exported by
  __vite-browser-external"). US-2 진행 기록에 남긴 "별도 CLI 스크립트가 명시되지 않아
  atomicWriteFundamentals를 fundamentalsSchema.js에 포함시켰다"는 설계 결정이 브라우저
  번들 포함 시점에 와서야 문제가 된 것 — research.json(v6)이 애초에 검증(researchSchema.js,
  browser-safe)과 원자적 쓰기(scripts/research/validate-research.mjs, Node 전용)를 분리해
  둔 이유가 바로 이것이었음을 뒤늦게 확인. atomicWriteFundamentals를 신설
  scripts/research/validate-fundamentals.mjs로 옮기고(validate-research.mjs와 동일
  구조), 관련 테스트도 scripts/research/validate-fundamentals.test.js로 이동해 동일하게
  맞춤 — src/lib/fundamentalsSchema.js는 이제 validateFundamentals 순수 함수만 export.
- 테스트 수 변화: 310 → 325 (+15, FundamentalBadge 0→7, FundamentalFailSection 0→5,
  Recommend 21→24, 파일 이동으로 fundamentalsSchema 12→8 / validate-fundamentals(신규) 0→4,
  합산 순증가는 위 내역대로), 전체 통과. npm run build 통과(206.21kB→212.87kB, 최초 build
  실패를 스키마 분리로 수정 후 재확인).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-12 (화면2 — 리서치 점검 배지 + 숨기기 토글). research.json이
  US-8에서 이미 riskFlags(v2)를 지원하고 researchLoader가 v1→[] 정규화까지 마쳐뒀으므로,
  이번 스토리는 순수 화면 배선. 3상태 배지("리서치 점검 ✓" / "⚠ 리스크 플래그 n건" /
  "리서치 미실시") — U-11의 FundamentalBadge/splitByFundamentalVerdict와 유사한 패턴으로
  research 배지 컴포넌트를 만들되, 이번엔 "숨기기"가 필요하므로 splitBy 방식이 아니라
  hideRiskFlagged 토글(localStorage v3, US-9에서 이미 필드 준비됨) 값에 따라 visible에서
  제외하는 필터링을 3개 뷰 공통으로 넣어야 함(자동 탈락이 아니라 사용자 토글이므로
  fundamentals의 "Fail 분리"와는 분기 로직이 다름 — 토글 꺼짐 시 항상 전부 표시).

## Iteration 12 — US-12. 화면2 — 리서치 점검 배지 + 숨기기 토글
- 완료 스토리: US-12
- 변경 파일:
  - src/lib/researchCheck.js(신설): computeResearchCheckState(research) — 순수 함수로
    분리해 컴포넌트(배지 렌더링)와 화면(숨기기 필터링) 양쪽에서 재사용. 반환
    { state: 'none'|'ok'|'flagged', flags }. **설계 보정**: PRD는 "✓"를 "플래그 0건 +
    sentiment≠negative"로만 정의하고 "플래그 0건인데 sentiment===negative"인 조합은
    정의하지 않았음 — 그대로 두면 부정적 센티먼트 종목이 아무 경고 없이 ✓로 통과하는
    결함이 되므로, 이 조합도 flagged(빈 flags 배열)로 안전하게 처리(US-5의 VCP 수식
    보정과 같은 종류의 "PRD 여집합 결함" 패턴).
  - src/lib/researchCheck.test.js(신설): 8개 테스트.
  - src/components/ResearchCheckBadge.jsx(신설): 3상태 배지 — "리서치 미실시"(회색)/
    "리서치 점검 ✓"(초록)/"⚠ 리스크 플래그 n건"(빨강, 펼치면 type 한글 라벨+description)
    /"⚠ 리서치 점검 필요"(flags 없이 negative sentiment인 보정 케이스, 펼치기 버튼 없음).
  - src/components/ResearchCheckBadge.test.jsx(신설): 5개 테스트.
  - src/screens/Recommend.jsx: filterHiddenRiskFlagged(list, researchMap, hideRiskFlagged)
    헬퍼 신설 — splitByFundamentalVerdict(분리+하단표시)와 달리 그냥 배열에서 제외만
    한다(별도 섹션 없음, "자동 탈락 아님" 원칙 그대로). 3개 뷰 전부 이 필터를
    fundamentals 분리 다음 단계로 체이닝(afterFundamentals → filterHiddenRiskFlagged →
    최종 list). ModeSegment 바로 아래에 "리스크 플래그 종목 숨기기" 체크박스를 모드와
    무관한 공통 위치로 추가(기본 꺼짐, hideRiskFlagged prop 기본값 false). 3개 뷰의
    researchSection 슬롯에 ResearchCheckBadge를 ResearchSection과 함께 묶어 전달
    (배지=요약, 섹션=상세 병존 원칙 그대로 SelectAndResearch API 변경 없이 구현).
  - src/App.jsx: toggleHideRiskFlagged 핸들러 추가, uiState.hideRiskFlagged/
    onToggleHideRiskFlagged를 Recommend에 전달.
  - 기존 테스트 수정: App.test.jsx의 두 e2e 테스트가 `getAllByRole('checkbox')`로 체크박스
    배열을 가져와 인덱스로 클릭하고 있었는데, 새 "숨기기" 토글 체크박스가 항상 목록 맨 위에
    먼저 렌더링되면서 인덱스가 한 칸씩 밀려 종목 선택 개수가 어긋나는 회귀가 발생 —
    두 테스트 모두 `getByRole('checkbox', {name: '리스크 플래그 종목 숨기기'})`로 토글을
    먼저 찾아 배열에서 제외하도록 수정(동작 자체는 무수정, 쿼리만 정교화).
  - 신규 테스트: Recommend.test.jsx에 3상태 배지 3개 + 숨기기 토글 3개(총 6개),
    App.test.jsx에 hideRiskFlagged localStorage 영속 통합 테스트 1개.
- 디버깅: 처음 작성한 테스트 픽스처(makeResearchMapWithFlags)가 summary 필드를 빼먹어
  ResearchSection의 firstSentence(undefined)가 TypeError를 던짐 — summary/catalysts/
  risks/sources/origin을 모두 채운 완전한 research 항목으로 수정. 또한 "배지가 전혀
  없어야 한다" graceful-degradation 테스트가 `/리스크 플래그/` 정규식으로 검사했는데
  이 문자열이 항상 렌더링되는 "리스크 플래그 종목 숨기기" 토글 라벨과도 매치돼 오탐 —
  `/⚠ 리스크 플래그/`(배지 전용 접두사)로 좁혀 수정.
- 테스트 수 변화: 325 → 345 (+20, researchCheck 0→8, ResearchCheckBadge 0→5,
  Recommend 24→30, App 12→13), 전체 통과. npm run build 통과(212.87kB→215.19kB).
- 운영자 액션 필요: 없음
- 다음 스토리 메모: US-13 (통합 검증 및 마감 — 코드보다 검증 중심). PRD §10 성공 기준
  7항목을 하나씩 점검해 progress.txt에 기록, README에 v8 운영 절차(주 1회 단일 스크립트
  실행→크기 확인→리서치 세션(선택)→커밋·배포) 갱신, 미충족 항목이 있으면 체크하지 말고
  해당 스토리로 되돌아갈 것. 이 스토리를 끝으로 US-1~US-13 전부 완료되면 v8 Ralph 루프
  종료 — 실데이터(2년 가격+fundamentals) 수집·research.json 재생성은 여전히 운영자가
  별도 python 세션에서 수행해야 함(코드는 전부 graceful degradation으로 준비됨, US-1/
  US-2에서 이미 기록된 동일 성격의 운영자 액션).

## Iteration 13 — US-13. 통합 검증 및 마감 (v8 Ralph 루프 마지막 스토리)
- 완료 스토리: US-13 (prd-v8-dual-mode_1.md의 US-1~US-13 전부 완료)
- 변경 파일:
  - PRD_Nasdaq8.md §10: 성공 기준 7항목을 하나씩 코드 근거와 함께 점검·표시.
    6항목은 [x](테스트로 검증된 완전 충족), 1항목("2년치 nasdaq100.json +
    fundamentals.json이 주 1회 단일 스크립트 실행으로 생성")은 [~](코드는 완성됐으나
    실제 실행은 Ralph 루프 절대 원칙상 이 루프가 수행하지 않으므로 운영자 액션 대기)로
    구분 표시 — "미충족 항목은 체크하지 말 것" 규칙을 글자 그대로 지키되, "코드는
    끝났지만 데이터 실행만 남음"이라는 상태를 [ ](완전 미착수)와 구별할 필요가 있어
    [~](부분 충족) 표기를 도입함.
  - README.md: 빈 제목 한 줄뿐이던 파일을 프로젝트 한 줄 요약 + 개발 명령어 +
    5단계 v8 운영 절차(collect_data.py 단일 실행 → 크기 확인/스키마 검증 →
    /research 선택 실행 → npm test/build 검증 → 커밋·배포)로 채움.
  - prd-v8-dual-mode_1.md: US-13 체크.
- 검증 내역(코드 레벨, 실데이터 수집 제외):
  1. 미너비니 8조건+VCP+7/8 완화+현금보유 배너 — minervini.test.js(28)+Recommend.test.js
     미너비니 뷰(4) 통과 확인. [x]
  2. 통합 뷰 ★★/★+백분위 평균 — consensus.test.js(6, "평균의 함정" 포함)+Recommend.test.js
     통합 뷰(3) 통과 확인. [x]
  3. 펀더멘털 허들 Pass/Partial/Fail+Fail 분리+결측 구분 — fundamentals.test.js(16)+
     FundamentalBadge/FundamentalFailSection(12)+Recommend.test.js(3) 통과 확인. [x]
  4. 리서치 배지 3상태+숨기기+v1 하위호환 — researchCheck.test.js(8)+
     ResearchCheckBadge.test.js(5)+Recommend.test.js(6)+researchSchema.test.js v1/v2(6)
     통과 확인. [x]
  5. localStorage v3 마이그레이션 — persistence.test.js(13, v1→v3/v2→v3 포함) 통과 확인. [x]
  6. 추세추종 단일 모드=v7 동등 — Recommend.jsx recommendMode 기본값 'trend' 설계로
     v7 시절 Recommend.test.jsx/App.test.jsx 테스트 전부 무수정 통과 재확인. [x]
  7. 2년 데이터+fundamentals 단일 스크립트 실행 — 코드 완성(US-1/US-2), 실제 실행은
     미착수(운영자 액션 필요). [~]
  - npm run test: 345개 전체 통과(이번 스토리에서 신규 테스트 추가 없음 — 검증 전용
    이터레이션이므로 기존 스위트 재확인만 수행). npm run build: 통과(215.19kB, 변화 없음
    — README/PRD 문서만 변경).
- 운영자 액션 필요 (v8 코드 완성 후 남은 항목, 전부 이 루프 범위 밖):
  1. **2년 실데이터 재수집**: 별도 python(yfinance/pandas) 세션에서
     `python scripts/collect_data.py` 1회 실행 → `public/data/nasdaq100.json`(2년치)과
     `public/data/fundamentals.json`을 함께 생성. 파일 크기 로그(10MB 경고 포함) 확인.
  2. **research.json 갱신(선택)**: `/research` 세션 실행 시 리스크 플래그를 남기려면
     schemaVersion:2로 저장(§4.5 작성 규칙 준수) — 플래그를 안 쓰면 기존처럼 v1도 무방.
  3. 재수집 후 `npm run test && npm run build`로 실데이터 기준 회귀 재확인 필요
     (특히 hasFullYearData(252일)·rsRawScore가 처음으로 실데이터 기준 값을 가지게 됨 —
     이번까지는 전부 합성 픽스처(nasdaq100.2y.sample.json)로만 검증됨).
  4. 재수집·재검증 완료 후 커밋·푸시(배포는 운영자 승인 필요, 이 세션에서 자동 실행 안 함).
- **PRD_Nasdaq8.md v8 — US-1~US-13 전부 완료.** 코드 레벨 성공 기준 7항목 중 6항목 완전
  충족, 1항목(실데이터 수집 실행)은 운영자 액션 대기 — 이는 v7 마감 시점과 동일한 종류의
  잔여 작업(v7도 "12개월 재수집은 운영자 수동 작업"으로 마감했음).

## Iteration 14 (운영자 세션, 루프 종료 후) — 2년 실데이터 재수집 + 회귀 재확인 + research.json 재생성
- 위 "운영자 액션 필요" 1~4번을 실제로 수행(python 3.13.14 + yfinance 1.5.1 + pandas 3.0.3
  로컬 사용 가능 확인 후 진행). 13개 v8 코드 커밋은 이 이터레이션 전에 이미 push됨.
- 1) `python scripts/collect_data.py` 실행: nasdaq100.json 100/100 티커 성공(0 실패),
  502거래일/티커(2년치), 파일 크기 8.07MB(10MB 경고 미발생). fundamentals.json 99/100
  티커 성공, CCEP만 "분기 재무표 없음"으로 excluded 처리. generatedAt: 2026-07-10.
- 2) 회귀 재확인(`npm run test`): 실행 직후 345개 중 2개 실패 — 둘 다 v7 12개월 재수집
  때와 동일한 "실데이터가 정당하게 바뀌어서 생기는 예상된 실패" 패턴:
  - `recommend.test.js`의 US-8 회귀 기준 스냅샷(`recommend-default-baseline.json`)이
    구 13개월 데이터 기준이라 새 2년 데이터의 실제 추천 결과와 달라짐 — node 스크립트로
    `recommend(buildDataset(new nasdaq100.json).tickers)`를 재실행해 픽스처 재생성(코드
    변경 없음, 데이터만 최신화).
  - `App.test.jsx`의 real-research.json 통합 테스트가 `within(axonCard).getByRole('button',
    {name:/펼치기/})`로 버튼을 찾았는데, 새 research.json(v2, riskFlags 포함)에서 AXON에
    리스크 플래그가 2건 생겨 US-12의 배지도 자체 "펼치기" 버튼을 갖게 되면서 쿼리가
    모호(ambiguous)해짐 — "AI 리서치" 섹션 버튼임을 명확히 하도록
    `{name:/AI 리서치/}`로 좁혀 수정(동작 자체는 무수정, 쿼리만 정교화).
  재수정 후 345개 전체 통과. `npm run build` 통과(215.19kB, README/PRD만 바뀐 이전
  빌드와 동일 — 데이터 파일은 번들에 안 들어감).
- 실데이터 기준 미너비니 파이프라인 최초 정상 동작 확인(node 스크립트로 직접 실행):
  level='strict'(완화 없이 8/8 조건 통과), 12개 종목 통과, insufficientSignal=false,
  excludedForInsufficientData=0(전 종목 502일≥252일 충족). 상위 3종목: FTNT 81.2 /
  MU 73.3 / AMD 64.9. 컨센서스 뷰에서 ★★(양쪽 모드 통과) 1종목 확인. v7 US-1의
  "251 vs 252일 오프바이원" 같은 실데이터 특유 버그는 이번엔 발견되지 않음(2년 수집이라
  버퍼가 충분해 경계값 문제가 발생하지 않은 것으로 추정).
- 3) `/research` 세션 실행: 재수집으로 추천 풀이 8종목(AXON/WDAY/FTNT/MDB/AAPL/PAYX/
  CTAS/DXCM)에서 10종목(AXON/META/WDAY/DDOG/PANW/CRWD/CDW/ZS/FTNT/ADP)으로 전면
  교체됨(완화 레벨 relaxed10d). 사용자가 추가로 MNST/CSX/SBUX 3종목 리서치를 요청 —
  `select-pool.mjs MNST CSX SBUX`로 나스닥100 소속·dataSufficient 여부 사전 검증 후 승인,
  세션 리서치 대상 총 13종목(15종목 상한 이내). 종목별 general-purpose 서브에이전트를
  병렬 실행(계층 A: EDGAR/Dataroma/Macrotrends/TipRanks/Zacks/Motley Fool/뉴스, 3-5회
  검색 상한, 매수/매도 권유 표현 금지, 사실 기반 소스 근거 필수)해 리서치를 수집하고
  `atomicWriteResearch`로 스키마 v2(riskFlags 포함) 검증 후 원자적 저장. 10개 추천 풀
  종목은 origin:'recommended'+signalPassed:true, MNST/CSX/SBUX는 origin:'userRequested'.
  리스크 플래그 발견 종목: AXON(소송+기타), META(실적임박+소송+규제), DDOG(기타),
  PANW(기타), CRWD(소송+규제), ZS(가이던스컷+기타), FTNT(실적임박), ADP(실적임박),
  MNST(실적임박), CSX(실적임박+규제), SBUX(실적임박+소송) — WDAY/CDW는 플래그 없음(✓).
  basedOnDataOf를 새 generatedAt(2026-07-10)과 동일하게 맞춰 staleness 경고 없음.
- 최종 상태: `npm run test`(345개 전체 통과) + `npm run build`(통과) 재확인 완료.
  변경 파일: public/data/nasdaq100.json, public/data/fundamentals.json,
  public/data/research.json, src/lib/__fixtures__/recommend-default-baseline.json,
  src/App.test.jsx(쿼리 정교화 1건), PRD_Nasdaq8.md(§10 항목1 [~]→[x]).
- 운영자 액션 필요: 이 이터레이션 결과물(실데이터 3종 JSON + 회귀 수정 2건 + PRD 갱신)의
  커밋·푸시 여부는 사용자 확인 후 진행.

# v9 진행 기록 (prd-v9-backtest.md) — 직접 순차 구현(Ralph 루프 아님, 사용자가
# "직접 순차 구현"을 선택), US-1~10 전부, 스토리마다 커밋 분리

## US-1. 백테스트 부트스트랩
- scripts/backtest.mjs가 src/lib/buildDataset·recommend·minervini를 그대로 import.
  동형성 테스트로 lib 직접 호출 결과와 완전히 일치함을 보증. 실데이터 CLI 스모크 확인.

## US-2. 시점 슬라이서
- scripts/lib/asOf.mjs: sliceUniverseAsOf(원본 불변, asOf 이하 봉만 절단) +
  buildEvaluationDates(워밍업252/말단여유60/스텝5). 경계·불변·짧은데이터 테스트 11개.

## US-3. 신호 재현 루프
- evaluateAsOf()/buildSignalRecords()/runSignalLoop() — 평가일마다 슬라이스 →
  recommend/runMinerviniRecommend/buildConsensusRanking(전부 기존 lib) → 신호 레코드.
  실데이터: 38개 평가일, 2567건 레코드.

## US-4. 성과 계산
- scripts/lib/performance.mjs: buildPriceIndex/computeSignalPerformance/aggregatePerformance.
  진입=신호일종가, 청산=N거래일후종가, 벤치마크=유니버스 등가중평균, 초과수익=차이.
  3종목 소형 픽스처 손계산 대조 11개 테스트.

## US-5. In/Out 분할 + backtest.json 발행
- runBacktest()가 평가일 전반50%(In)/후반50%(Out) 분할(splitDate 당일→Out 귀속),
  PRD §7 스키마로 backtest.json 발행. src/lib/backtestSchema.js(브라우저 세이프) +
  scripts/validate-backtest.mjs(원자적 쓰기, research/fundamentals와 동일 패턴).
  실데이터: 16개 strategies 항목, splitDate=2025-11-25.

## US-6. 펀더멘털 축 근사 재구성
- scripts/lib/fundamentalHistory.mjs: quarters[]를 분기i vs 직전분기(QoQ 근사)로
  재구성해 evaluateFundamentalHurdle()을 그대로 호출(재구현 없음). F5(ROE)는 현재
  스냅샷 — 선견 편향 note("근사 재구성 · 짧은 구간 참고치")로 고지. coveredFrom 이전
  신호는 집계에서 제외. 실데이터: pass 7 / partial 178 / fail 43건 교차표 생성.

## US-7. 후보 변형 비교 프레임 + 변형 A/B/C
- scripts/lib/variants.mjs: A(disparity_inverted_u)/B(adx_gate)/C(consensus_weighted).
  recommend.js(stage1Pass/scoreTicker/SCORE_*)·consensus.js(percentileOf)에 export만
  보강(동작 불변, 기존 회귀 그대로 통과). ADX는 scripts/lib/adx.mjs에만(앱 미반영).
  adopted 항상 false. 실데이터로 3변형 모두 실행 확인(delta 기록됨).

## US-8. 신뢰도 표시 UI
- src/lib/backtestLoader.js(graceful degradation) + src/components/BacktestConfidence.jsx
  (요약+고정 고지 문구 분리불가 렌더, 펼침 상세, Out만 사용). Recommend.jsx 3모드에 배선,
  DesignValueNotice가 backtest.json 존재 시 문구 교체. 기존 회귀 전부 유지.

## US-9. 데이터 수집 3년 확대
- collect_data.py PERIOD 2y→3y(백테스트 워밍업+평가2년+보유60일 여유 근거 주석 갱신).
  결정적 3y 합성 픽스처(756거래일) 추가, 2y/3y 양쪽 엔진 완주 확인. 실데이터 재수집은
  운영자 별도 세션(루프 범위 밖, Out of Scope 원칙 그대로 유지).

## US-10. 워크플로 문서화 + 통합 검증
- README.md에 "백테스트 & 파라미터 조정 워크플로" 절 추가(5단계: 실행→In 근거 조정안→
  constants/v8.js 수정+changelog→재실행으로 Out 확인→커밋, 자동최적화 금지·Out 악화시
  롤백 명기). constants/v8.js 상단에 changelog 템플릿 추가(값 변경 없음). 운영 절차에
  "백테스트 재실행"(4번, 권장: 분기1회+파라미터변경시) 단계 삽입.

### PRD_Nasdaq9.md §8 성공 기준 점검
1. [x] backtest.mjs가 앱 lib를 그대로 import, 백테스트 전용 재구현 코드 없음 (US-1/3, 매 스토리 재확인)
2. [x] 시점 재현 테스트: t 이후 데이터 개입 불가가 슬라이스 경계 테스트로 보증 (US-2, 11개 테스트)
3. [x] 4개 전략 축 × In/Out × 보유 3종 성과표가 backtest.json으로 발행되고 스키마 검증 통과 (US-5)
4. [x] 컨센서스 ★★ vs ★, 펀더멘털 Pass/Partial/Fail 실측 비교표 산출 (US-4/US-6, 실데이터로 확인됨)
5. [x] 파라미터 조정 워크플로가 README 문서화 + constants/v8.js changelog 주석 (US-10)
6. [x] 화면2 신뢰도 표시가 Out-of-Sample만 사용, 고정 고지 문구 동반, backtest.json 부재시 완전 미표시 (US-8)
7. [~] 변형 결과는 variants[]에 전부 기록되고 adopted는 항상 false (US-7) — 단, "채택 시 화면에
       스코어링 변경 안내 1줄" UI는 미구현. 이번 세션에서 어떤 변형도 실제로 채택(operator 승인)된
       적이 없으므로, 조건부로만 필요한 UI를 지금 미리 만드는 것은 추측성 선반영이라 보류했다.
       실제로 한 변형이 채택되는 다음 세션에서, 그 변형에 한해 안내 문구를 추가하는 별도 스토리로
       처리할 것 (Out of Scope 원칙: "채택된 변형의 앱 반영은 운영자 결정 후 별도 작업").
8. [x] 기존 테스트 전체 통과 — 434개 전체 통과, npm run build 매 스토리마다 확인.

- 실데이터 검증: scripts/backtest.mjs(fundamentals.json 자동 인식)를 실제
  public/data/nasdaq100.json(100종목·2년) 기준으로 실행해 backtest.json 스키마 통과,
  16개 strategies + fundamentalAxis(pass/partial/fail) + variants(3종) 전부 정상 산출
  확인(임시 경로에만 저장, 저장소에는 커밋하지 않음 — 실데이터 backtest.json 커밋은
  research.json/fundamentals.json과 동일하게 사용자 확인 후 별도로 진행할 것).
- 운영자 액션 필요: (1) collect_data.py 3y 재수집, (2) node scripts/backtest.mjs로
  실제 backtest.json 생성, (3) 두 데이터 커밋 여부 확인 후 진행 — 전부 이번 세션
  범위 밖(코드만 완성, 코드 스토리는 픽스처까지만이라는 US 문서의 명시적 원칙).

- 후속(같은 날, 사용자 확인 후): 10개 v9 커밋 푸시·배포 확인. python scripts/collect_data.py
  실제 실행(3년치, 100/100종목, 707~753거래일, 12.09MB — 10MB 경고 최초 발생) +
  fundamentals.json(99/100종목). node scripts/backtest.mjs로 실제 backtest.json 최초 생성
  (평가일 89개, Out 표본 157~225건, 변형 3종 모두 채택기준 미충족). 부수 발견: collect_data.py가
  Windows cp949 콘솔에서 10MB 경고 로그의 em dash(—) 출력 시 UnicodeEncodeError로 죽는
  버그 발견·수정(stdout/stderr UTF-8 강제). recommend-default-baseline.json 재생성(데이터
  변경에 따른 정당한 결과). npm test(434개)+build 재확인 후 커밋·푸시·배포 확인 완료.

# v9.1 진행 기록 (prd-v9.1-diagnostics.md) — Ralph 루프(/loop 스킬, 동적 자기 페이싱)

## Iteration 1 — US-1. backtest.json 스키마 v2 — 완화/정상 분리 집계
- 완료 스토리: US-1
- 변경 파일: src/lib/backtestSchema.js(SCHEMA_VERSION 1→2, SUPPORTED_SCHEMA_VERSIONS=[1,2],
  signalQuality 검증은 v2에서만), src/lib/backtestLoader.js(v1→v2 정규화: signalQuality
  없으면 "all" 채움, findStrategy에 signalQuality 파라미터 추가·기본값 "all"),
  scripts/backtest.mjs(SIGNAL_QUALITIES=[all,normal,relaxed], filterByQuality, runBacktest이
  각 (key,basis,sample)마다 3종 신호 품질 전부 발행, schemaVersion:2로 변경, 콘솔에
  formatSignalQualityComparison 추가), 테스트 4개 파일.
- 테스트 수 변화: 434 → 442 (+8: backtestSchema 4개, backtestLoader 1개, backtest.mjs 3개)
- 실데이터 검증: node scripts/backtest.mjs 실행 결과 — 가설 ① 판정 재료 확보:
  in/normal 승률50.0%·초과수익+0.07%p(226건) vs in/relaxed 50.4%·-0.09%p(119건);
  out/normal 48.6%·+1.82%p(257건) vs out/relaxed 40.3%·+0.56%p(72건). Out 구간에서
  완화 신호가 정상 신호보다 뚜렷이 나쁨(승률 8.3%p 격차) — 가설 ①에 대한 실측 근거 확보,
  최종 판정(완화 신호 top5 제외 여부)은 US-6 판정 기준표 + 운영자 결정 몫으로 남김.
  임시 경로에만 저장, public/data/backtest.json은 아직 갱신 안 함(US-6 마감 후 재생성 예정).
- 다음 스토리 메모: US-2 (변형 D 청산 규칙 — scripts/lib/exits.mjs 신규, performance.mjs
  확장, 손절−8%+시간/트레일링 두 변형, 손계산 픽스처 테스트).

## Iteration 2 — US-2. 변형 D 청산 규칙 (경로 의존 성과 계산)
- 완료 스토리: US-2
- 변경 파일: scripts/lib/exits.mjs(신규 — walkExit/computeExitPerformance/aggregateExitPerformance,
  EXIT_RULES={exit_stop8_time60, exit_stop8_trail15}), scripts/lib/performance.mjs(entryPoint/
  maxDrawdown/average/median/round4에 export만 보강 — 동작 불변, exits.mjs가 재구현 없이
  재사용), scripts/backtest.mjs(evaluateExitVariants 추가, variants[]에 A/B/C 뒤에 D 2종
  덧붙임), src/lib/backtestSchema.js(validateVariant에 선택 필드 outDetail 검증 추가),
  scripts/lib/exits.test.js(신규, 11개), scripts/backtest.test.js(+4, 기존 US-7 3종
  단언을 arrayContaining으로 완화).
- 테스트 수 변화: 442 → 457 (+15)
- 실데이터 검증: node scripts/backtest.mjs 실행 — Out 표본 215건(trend·top5) 기준
  exit_stop8_time60(avgHoldingDays 44.0, stopHitRate 40.5%, avgExcess 6.85%p) /
  exit_stop8_trail15(avgHoldingDays 39.5, stopHitRate 59.1%, avgExcess 3.72%p) 모두
  현행 60일 고정 보유 대비 avgExcessDelta·winRateDelta가 음수 — 가설 ②("60일 엣지
  보존 + MDD 개선") 미충족으로 실측됨(이번 실데이터 스냅샷 기준. 손절·트레일링 채택은
  운영자 판단, adopted 항상 false 유지). 임시 경로에만 저장, public/data/backtest.json은
  아직 갱신 안 함(US-6 마감 후 재생성 예정).
- 다음 스토리 메모: US-3 (stepDays 파라미터화 — --step/--out CLI 인자, overlapFactor,
  step≠5 시 --out 필수).

## Iteration 3 — US-3. stepDays 파라미터화 + 겹침 보정 명시
- 완료 스토리: US-3
- 변경 파일: scripts/backtest.mjs(parseArgs/validateCliArgs export, main()이 --step=N/--out=경로
  named 플래그 파싱, step≠5면 --out 필수 가드, computeOverlapFactor로 config.overlapFactor
  기록, runSignalLoop에 onProgress 콜백 추가(기본 no-op, 테스트 오염 없음), CLI가 평가일
  10% 단위 진행률 로그 + formatOverlapFactorNote 콘솔 출력), src/lib/backtestSchema.js
  (config.overlapFactor 선택 필드 검증 추가), scripts/backtest.test.js(+13).
- 테스트 수 변화: 457 → 468 (+11, 순감 있음 — 기존 검증 중 일부를 새 테스트로 대체)
- 실데이터 검증: `node scripts/backtest.mjs public/data/nasdaq100.json --step=1
  --out=<temp>` 실행 — 441개 평가일(step=5의 89개 대비 약 5배), 진행률 로그 10회 정상 출력,
  "겹침 보정(20거래일 기준): 명목 표본 1068 / 유효 독립 표본 근사 53.4(overlapFactor=20)"
  정상 출력. 가드 확인: --step=1만 주고 --out 생략 시 거부(exit 1), --step=15(범위 밖)도
  거부. 실행 시간 1분38초(step=5 대비 약 4.5배 — "5배 연산량" 예상과 부합). 소요 시간이
  길어 사용자가 외출하게 되어 이 스토리까지만 커밋하고 세션 중단.
- 다음 스토리 메모: US-4 (신호 신선도 코호트 분석 — scripts/lib/freshness.mjs 신규,
  골든크로스/피벗돌파 기반 코호트, backtest.json에 freshnessCohorts[] 추가).

## Iteration 4 — US-4. 신호 신선도 코호트 분석
- 완료 스토리: US-4
- 변경 파일: scripts/lib/freshness.mjs(신규 — goldenCrossFreshnessDays는 goldenCrossWithin(N)의
  창을 하나씩 늘려가며 처음 true가 되는 지점을 daysAgo로 환산, pivotBreakoutFreshnessDays는
  series를 끝에서부터 하루씩 잘라 pivotProximity(===0이면 그날이 직전 63거래일 신고가)를
  재호출 — 둘 다 판정 조건 자체는 한 줄도 새로 쓰지 않고 기존 함수 재호출만으로 daysAgo를
  구함, freshnessCohort()로 0d/1-2d/3-4d/5d+/no_recent_breakout 배정, aggregateFreshnessCohorts로
  (key,cohort)별 byHolding 집계), scripts/lib/freshness.test.js(신규, +16 — 크로스/돌파일을
  아는 합성 픽스처로 5개 코호트 전부 검증 + lookback 밖 no_recent_breakout + 63일 미만 가드),
  scripts/backtest.mjs(buildSignalRecords가 datasetTickers(선택, 하위호환) 인자를 받아
  trend/minervini 레코드에만 freshnessCohort 부여 — 컨센서스는 이벤트 미정의라 제외,
  runSignalLoop이 dataset.tickers를 threading, runBacktest가 freshnessCohorts(in/out) 발행,
  formatFreshnessCohortSummary 콘솔 출력), src/lib/backtestSchema.js(freshnessCohorts 선택
  필드 검증 — key는 trend/minervini만 허용), scripts/backtest.test.js(+8, 완전성·배타성
  실데이터 검증 포함), src/lib/backtestSchema.test.js(+4), src/lib/backtestLoader.test.js(+1).
- 스코프 결정: freshnessCohorts는 trend/minervini 2개 전략만 대상 — PRD가 이벤트를
  모드별로만 정의했고(추세추종=골든크로스, 미너비니=피벗돌파) 컨센서스는 두 모드 신호가
  섞여 단일 이벤트를 정의할 근거가 없음. lookback 상한(10거래일)은 미너비니 쪽 PRD 명시값을
  추세추종에도 대칭 적용(코호트 축을 공유하므로).
- 테스트 수 변화: 468 → 497 (+29)
- 실데이터 검증: `node scripts/backtest.mjs public/data/nasdaq100.json --out=<temp>` 실행,
  Out 구간 코호트별 20/60일 초과수익표 정상 출력 — 예: trend 0d(표본62) 20일 +0.28%p,
  1-2d(표본106) +2.22%p, 3-4d(표본98) +2.28%p, 5d+(표본58) +1.72%p,
  no_recent_breakout(표본5, 매우 작음) -13.65%p. minervini는 0d(표본192) +0.44%p ~
  3-4d(표본107) +2.92%p로 완만한 우상향 경향. 표본 크기 편차가 커서(특히
  no_recent_breakout) 가설 ③ 최종 판정은 README 판정 기준표(US-6)에서 운영자가 내림 —
  이 스토리는 측정치 산출까지만.
- 다음 스토리 메모: US-5 (화면2 신뢰도 표시 — 20일·60일 병기, BacktestConfidence.jsx +
  getConfidenceSummary 확장, overlapFactor 있으면 유효 표본 주석 병기).

## Iteration 5 — US-5. 화면2 신뢰도 표시 — 20일·60일 병기
- 완료 스토리: US-5
- 변경 파일: src/lib/backtestLoader.js(getConfidenceSummary가 holding60/insufficientSample60/
  effectiveSample60(=holding60.signals/config.overlapFactor[60], overlapFactor 없으면 null)을
  추가 반환 — 기존 필드(strategy/holding20/insufficientSample)는 이름·의미 불변으로 유지해
  하위 호환), src/components/BacktestConfidence.jsx(SummaryLine을 "검증 구간(Out-of-Sample)
  초과수익 — 20거래일 +n.n%p · 60거래일 +n.n%p (겹침 보정 유효 표본 약 n건) (라벨 · 상위
  5종목 기준)" 형태로 재작성 — 기존 승률 중심 1줄 요약을 대체, MODE_LABELS로 추세추종/
  미너비니/★★ 컨센서스 라벨 분기), src/lib/backtestLoader.test.js(+3),
  src/components/BacktestConfidence.test.jsx(기존 옛 텍스트 검증 1건을 새 포맷으로 교체,
  +3 신규).
- 설계 결정: 20거래일 레코드 부재 시 전체 null 반환하는 기존 하드 게이트(US-8)는 그대로 두고,
  60거래일 레코드만 없는 경우(구버전 픽스처 등)는 insufficientSample60:true로만 표시하고
  전체를 null 처리하지 않음 — 기존 "표본 0" 테스트 픽스처(20일만 있는 zeroSample)가 여전히
  유효한 summary를 반환해야 하므로 이 비대칭이 필수였음.
- 테스트 수 변화: 497 → 503 (+6)
- 실데이터 검증: public/data/backtest.json(스키마 v1, US-1~4 이전에 생성되어 overlapFactor·
  freshnessCohorts 둘 다 없음)을 getConfidenceSummary에 직접 통과시켜 확인 — trend/minervini/
  consensus 전부 holding60 정상 반환(예: trend 60일 +11.31%p), insufficientSample60:false,
  effectiveSample60:null(하위 호환 정확히 동작). npm run build 성공.
- 다음 스토리 메모: US-6 (통합 검증·마감 — README v9.1 진단 실행 가이드 + 3가설 판정
  기준표, progress.txt 최종 점검, PRD 체크박스 마감).

## Iteration 6 — US-6. 통합 검증·마감 (v9.1 전체 마감)
- 완료 스토리: US-6 (US-1~6 전부 완료, prd-v9.1-diagnostics.md 체크박스 전부 [x])
- 변경 파일: README.md("v9.1 진단 실행 가이드" 절 신규 — ① step=5 공식 실행 ② step=1 실험
  실행(--out 필수 가드 재설명) ③ 세 가설(완화 신호 오염/청산 규칙 개선 여지/신선도 프리미엄)
  판정 기준표, "판정은 항상 운영자 몫" 재명기), progress.txt(본 항목), prd-v9.1-diagnostics.md
  (US-6 체크).
- v9.1 스토리별 최종 점검:
  - US-1(완화/정상 분리 집계): ✓ — signalQuality 3종 all/normal/relaxed, normal+relaxed=all
    항등식 전 조합 테스트로 보증.
  - US-2(청산 변형 D): ✓ — exit_stop8_time60/exit_stop8_trail15, adopted 항상 false,
    src/ 미변경 확인 테스트 포함.
  - US-3(stepDays 파라미터화): ✓ — --step/--out, step≠5시 --out 필수 가드, overlapFactor,
    실데이터로 step=1 완주 확인(이전 이터레이션에서 검증 완료).
  - US-4(신호 신선도 코호트): ✓ — freshnessCohorts[], trend/minervini만 대상(컨센서스
    이벤트 미정의로 스코프 제외), 완전성 테스트를 실데이터로 확인.
  - US-5(20·60일 병기): ✓ — BacktestConfidence 요약 재구성, overlapFactor 부재 시 하위
    호환, 실제 production backtest.json(v1)으로 검증.
  - US-6(통합 검증·마감): ✓ — 본 항목.
- 이 루프 범위에서 의도적으로 하지 않은 것(Out of Scope, PRD 명시): 운영 주기 변경(매일
  수집·GitHub Actions 자동화 — 가설 ③ 판정 후 별도 PRD), constants/v8.js 값 변경, 변형/청산
  규칙의 채택(adopted는 전부 false 유지), 완화 폴백 로직 변경, 거래비용 실제 모델링,
  step=1 실데이터 실행(README 가이드만 작성 — 실행 자체는 운영자가 별도 세션에서 수행).
- 미해결(운영자 액션, 다음 세션 후보):
  1. `node scripts/backtest.mjs --step=1 --out=<경로>` 실데이터 실행 → 콘솔의 신선도
     코호트 표로 가설 ③ 실제 판정.
  2. `node scripts/backtest.mjs`(step=5, 공식) 재실행 → US-1~5가 추가한 signalQuality/
     freshnessCohorts/overlapFactor 필드를 실제 반영한 새 `public/data/backtest.json` 생성·
     검증·커밋 (현재 배포본은 v9.1 이전 스키마 v1 — 하위 호환으로 정상 동작하지만, 새 필드
     기반 화면 표시·가설 판정은 재실행 전까지 반영되지 않음).
  3. 위 실행 결과로 README 판정 기준표 3가설을 실제로 판정 → 필요 시 후속 PRD(완화 신호
     제외/청산 규칙 채택/매일 운영 전환) 착수 여부 결정.
  4. 이 세션의 v9.1 커밋 3개(US-3~US-5 관련 신규 커밋 + 이전 세션 US-1~US-2 커밋 포함,
     총 6개)를 푸시하기 전 사용자 확인.
- 테스트 수 변화: 503 (US-6은 문서 전용 스토리라 테스트 추가 없음) — npm run test/build
  둘 다 최종 통과 확인.

# v10 Ralph 루프 진행 기록 (prd-v10-pipeline.md) — 직접 순차 구현 (사용자 선택)

## Iteration 1 — US-1. NGX 수집 파이프라인
- 완료 스토리: US-1
- 변경 파일:
  - scripts/collect_data.py: `--universe=ndx|ngx` 인자(argparse) 추가, 기존 main()을
    collect_ndx()로 이름 변경(동작 완전 동일, 회귀 없음), collect_ngx()/정규 함수 3종
    (normalize_ticker/is_non_nasdaq_line/load_ngx_ticker_source/passes_liquidity_guardrail)
    신규, collect_fundamentals()에 out_path 파라미터 추가(ndx/ngx 공용).
  - scripts/test_collect_data.py(신규): 표준 unittest 14개 — 가드레일 경계(정확 $10/$20M,
    미만 2종, 빈 데이터, 20일 윈도우만 사용) 5개, 티커 정규화 3개, 이중상장 제외 3개,
    CSV 로더(정규화 포함/제외/중복제거) 3개. 저장소가 pytest 등 별도 프레임워크를 쓰지
    않으므로 신규 의존성 없이 stdlib unittest로 작성(npm run test와 별도 게이트,
    `python -m unittest scripts/test_collect_data.py -v`로 수동 실행).
  - src/lib/__fixtures__/ngx100.sample.json(신규): 결정적(sine+trend) 3티커×756일 합성
    픽스처 + excluded[](가드레일 미달 2종·이중상장 1종 예시).
  - src/lib/buildDataset.test.js(신규 — 기존에 buildDataset 전용 테스트 파일이 없었음):
    NGX 픽스처가 나스닥100과 동일 스키마({generatedAt,tickers[{ticker,name,sector,series}]})
    이고 buildDataset()이 에러 없이 처리함을 확인(AC4 스키마 동일성).
- 승인 기준 확인:
  1. `--universe` 미지정 = collect_ndx() 그대로 호출, 기존 로직 한 글자도 안 바뀜 — 확인.
  2. 가드레일 경계 테스트 5개(요구 4개 이상) — 통과.
  3. 정규화·이중상장 단위 테스트 6개(요구 3개 이상, 정규화 3 + 이중상장 3) — 통과.
  4. NGX 픽스처 스키마 동일성 테스트 통과.
- 설계 결정: NGX 티커 소스는 로컬 CSV 경로(`--ngx-source`, 기본
  `scripts/ngx_holdings.csv`)로 받는다 — 실제 QQQJ 보유종목 공시 CSV 준비는 운영자 몫
  (Out of Scope, PRD 명시: "실데이터 수집·실데이터 백테스트 실행"). CSV 부재 시 에러 로그
  후 종료(exit 1), 크래시하지 않음 — 확인.
- 테스트 수 변화: 503 → 506 (JS 3개 추가) + Python stdlib unittest 14개(별도 게이트,
  npm 카운트에 미포함). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-2 국면 지표 lib(src/lib/regime.js) — breadth()는 buildDataset()의
  dataSufficient 필터링된 유니버스를 그대로 재사용.

## Iteration 2 — US-2. 국면 지표 lib
- 완료 스토리: US-2
- 변경 파일:
  - src/lib/constants/regime.js(신규): REGIME_UP_ENTER(0.65)/REGIME_UP_EXIT(0.55)/
    REGIME_DOWN_ENTER(0.40)/REGIME_DOWN_EXIT(0.50)/REGIME_SMA_PERIOD(200), PRD §4.2 표 그대로.
  - src/lib/regime.js(신규): `breadth(tickers)`(최신 시점 스냅샷), `regimeSeries(tickers)`
    (일자별 국면 시계열), `currentRegime(tickers)`(최신 유효 국면+전환일), 내부 헬퍼
    `applyHysteresis(dates,values)`(상태기계 자체를 breadth 계산과 분리 — 히스테리시스
    단위 테스트가 실제 유니버스를 조립하지 않고도 임의 breadth 시퀀스로 검증 가능해짐).
    breadth 계산은 기존 indicators.js의 sma()를 재사용(재구현 없음), 날짜 문자열로
    티커 간 정렬(종목마다 상장일이 달라 길이가 다를 수 있으므로 인덱스가 아닌 date 매칭).
  - src/lib/regime.test.js(신규): 12개 테스트 — 히스테리시스 데드존/상승유지 오실레이션·
    실제 전이·결측일 상태 보존(AC1, 4개), 초기 상태 3경계(AC2, 3개),
    dataSufficient=false 제외·SMA200 워밍업 불가(150일) 종목 제외·전량 제외 시 null·
    currentRegime/regimeSeries 워밍업 구간 처리(AC3, 5개).
- 승인 기준 확인: AC1(4개 요구, 4개 작성) 통과, AC2(경계 3종) 통과, AC3(dataSufficient
  반영 3종 + 통합 2종) 통과.
- 설계 결정: 히스테리시스 상태는 breadth 계산 불가일(null)에도 리셋되지 않고 다음 유효일로
  이어짐 — 초기 판정으로 되돌아가면 결측 하루마다 국면이 튈 위험이 있어 명시적으로 방지.
  초기 상태 분류는 진입(enter) 문턱이 아니라 유지(exit) 문턱(0.55/0.40)을 경계로 사용
  (PRD §4.2 표 그대로 — 진입 문턱 0.65/0.40이 아님에 주의).
- 테스트 수 변화: 506 → 518 (+12). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-3 진입가 엔진 1/2(src/lib/entryPoint.js) — design-entry-point-engine.md
  §2(피벗)·§3(파생가격) 그대로 구현, PH_d는 당일 제외 63일 최고 종가.

## Iteration 3 — US-3. 진입가 엔진 (1/2) — 피벗 산정 + 파생 가격
- 완료 스토리: US-3
- 변경 파일:
  - src/lib/constants/entry.js(신규): PIVOT_LOOKBACK(63)/BREAKOUT_RECENCY(21)/BUFFER(0.3%)/
    CHASE_LIMIT(5%)/FAR_BAND(10%)/STOP_PCT(8%)/ATR_STOP_MULT(2.5)/VOL_MULT(1.5) — 설계서 §9 표 그대로.
  - src/lib/entryPoint.js(신규): `computePivot(series)`(§2.1 3규칙 — 머리 위 저항선/최근
    돌파된 저항선/저항선 소멸), `derivedPrices({pivot,currentClose,atr14})`(§3 — 트리거/
    유효상단/원거리경계는 pivot 기준, 손절 참고 2종은 현재 종가 기준).
  - src/lib/entryPoint.test.js(신규): 13개 — 데이터부족·규칙1·당일제외·규칙2·21일 경계
    (정확21=유효/22=무효)·규칙3(장기상승 소멸)·중복돌파 미기록 확인(AC1, 7개 — 요구 6개
    이상), derivedPrices null전파·상수기반 산출·손절2종·atr14 미제공(AC3 등, 6개).
- 승인 기준 확인: AC1(피벗 3규칙 픽스처, 요구 6개 이상 → 7개) 통과, AC2(당일 제외) 통과,
  AC3(매직넘버 0 — 전부 constants/entry.js 값만 사용) 통과.
- 설계 결정(코드 결정, 채택 아님): 21거래일 내 돌파 이벤트가 다수면 가장 최근 이벤트를
  사용(PRD가 명시하지 않아 "현재와 가장 관련 있는 저항선"이라는 취지로 판단). 손절 참고
  2종의 앵커는 pivot이 아니라 "현재 종가"(prd-v10-pipeline.md US-3 구현 지시 원문
  "close − 2.5×ATR14" 그대로) — 실제 포지션의 체결가 기준 손절은 US-6 positionPlan.js가
  별도로 담당(진입 전 종목에는 아직 체결가가 없으므로).
- 테스트 수 변화: 518 → 531 (+13). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-4 진입가 엔진 2/2 — entryPoint.js에 상태 판정(0~3) 함수 추가,
  §5 의사코드 그대로(computePivot 재사용, 재구현 없음), 거래량 확인(SMA50 volume,
  기존 volumeTrend와 다른 함수 필요 — SMA50은 indicators.sma() 재사용), 돌파 후 경과일.

## Iteration 4 — US-4. 진입가 엔진 (2/2) — 상태 판정 + 거래량 확인 + 경과일
- 완료 스토리: US-4
- 변경 파일:
  - src/lib/entryPoint.js: `judgeEntryState(series)` 추가 — computePivot()을 재호출해
    재구현 없이 §5 의사코드 그대로 상태 0~3/산정불가 판정, 거래량 확인(SMA50(volume)은
    indicators.sma() 재사용), daysSinceBreakout·earlyBreakout(0~1일) 산출.
  - src/lib/entryPoint.test.js: 13개 추가(총 26개) — 상태 경계(FAR_BAND 0.90P 정확·직하·
    피벗P 정확·유효상단U 정확·직상·갭업, AC1 6개), 경과일·earlyBreakout·volumeOK
    조합(daysSinceBreakout값·경과0일true·경과15일false·volumeOK true/false, AC2 5개),
    엣지케이스(산정불가·저항선소멸, AC3 2개 — 무거래량돌파는 AC2의 volumeOK=false
    테스트가 겸함).
- 승인 기준 확인: AC1(6개 요구, 6개) 통과, AC2(4개 요구, 5개) 통과, AC3(각 1개 이상) 통과.
- 테스트 픽스처 관련 함정(기록 가치 있음): "돌파 후 평평하게 유지되다 마지막 날만 다른 값"
  식 픽스처는 의도와 달리 마지막 날 자체가 "새로운 돌파 이벤트"로 잡혀버린다 — 평평한
  구간이 길어지면 PH_t가 그 평평값까지 따라잡고, 마지막 날의 근소한 차이가 다시
  "직전일 ≤ PH, 오늘 > PH" 크로스 조건을 만족시키기 때문. 픽스처는 반드시 ①단일 돌파일이
  곧 오늘이거나(daysSinceBreakout=0 고정) ②돌파 이후 매일 완만히 계속 상승(정체 없음, 이 경우
  전일 종가가 항상 전일 PH보다 높으므로 재크로스가 안 생김) 둘 중 하나로 구성해야 한다.
- 테스트 수 변화: 531 → 544 (+13). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-5 청산 신호 계층 1(src/lib/exitSignals.js) — X3(템플릿 붕괴)는
  minervini.js의 evaluateTrendTemplate() 그대로 재사용(재구현 금지, 조건 미충족 개수 집계).

## Iteration 5 — US-5. 청산 신호 계층 1 (종목 수준)
- 완료 스토리: US-5
- 변경 파일:
  - src/lib/constants/exit.js(신규): X1_STRONG_VOLUME_MULT/X2_RECENCY_DAYS/
    X3_BREAKDOWN_MIN_MISSING/X4_RETURN_10D_MIN_PCT/X4_SMA50_DISPARITY_MIN_PCT/
    X5_RECENCY_DAYS (X5 거래량 배수는 별도 상수 없이 constants/entry.js의 VOL_MULT 공유).
  - src/lib/entryPoint.js: `findBreakoutEvents(series)` 신규 export — 기존 내부
    precedingHighSeries/breakoutIndices를 그대로 노출한 공용 헬퍼(재구현 없이 X5가 재사용).
  - src/lib/exitSignals.js(신규): `evaluateExitSignals(series,{rsPercentileValue})` —
    X1(50일선 이탈, sma() 재사용)·X2(데드크로스 — goldenCrossWithin(ema26,ema12,5)를
    인자 순서만 뒤집어 재사용, 재구현 없음)·X3(템플릿 붕괴 — minervini.evaluateTrendTemplate
    직접 호출)·X4(클라이맥스 런 — disparity() 재사용)·X5(돌파 후 최대 낙폭일 —
    findBreakoutEvents 재사용) 판정, 전부 "조건 충족" 사실 서술 evidence 문자열 반환.
  - src/lib/exitSignals.test.js(신규): 20개 — X1 경계(SMA50 정확/직하·강도2종, 4개),
    X2 최근5일 경계(발생전·당일·경계4일·경계5일밖, 4개), X3(재사용 확인·데이터부족·
    건강/붕괴·3개경계트리거·2개경계미트리거, 6개), X4(트리거·미트리거, 2개), X5(트리거·
    거래량미동반·경과6일밖·돌파이력없음, 4개).
- 승인 기준 확인: AC1(경계 테스트 10개 이상 → 실질 14개) 통과. AC2(X3이 evaluateTrendTemplate
  실호출 — strength 로직이 실제 missingConditions 배열에 의존하므로 재구현이면 통과 불가능한
  테스트로 확인) 통과. AC3(금칙어 테스트 — 최초 X4 evidence 문구에 "매도"가 실제로 들어있어
  테스트가 잡아냄 → "클라이맥스 런 패턴 감지"로 즉시 수정, 테스트가 의도대로 작동한 사례).
- 테스트 픽스처 관련 함정(US-4와 유사): 데드크로스(X2) 테스트에서 EMA는 인과적(과거 데이터만
  의존)이므로 시리즈를 다른 길이로 자르면 "오늘"만 바뀔 뿐 과거 인덱스의 EMA 값은 그대로 —
  이 성질을 이용해 정확한 크로스 발생 인덱스를 먼저 스크립트로 역산한 뒤 길이별 경계
  테스트(발생 전/당일/+4일/+5일)를 구성했다. X3의 "T1/T5 미포함 3개 붕괴(중)" 케이스는
  실제 트렌드 템플릿 조건들이 서로 강하게 상관되어(T1·T4·T5가 대체로 함께 무너짐) 자연스러운
  픽스처로 구성하기 어려워 별도 테스트 없이 강도 로직(2줄 조건문)은 코드 리뷰로 갈음.
- 테스트 수 변화: 544 → 564 (+20). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-6 포지션 계획 계층 2(src/lib/positionPlan.js) — 손절 참고 2종의
  검증 상태 매핑은 constants/verification.js 신규(코드 문자열→상태), entryPoint.js의
  derivedPrices()/STOP_PCT/ATR_STOP_MULT 재사용.

## Iteration 6 — US-6. 포지션 계획 계층 2 (포지션 수준)
- 완료 스토리: US-6
- 변경 파일:
  - src/lib/constants/verification.js(신규): VERIFICATION_STATUS — stopFixed8pct(열위)/
    stopAtr(측정중)/holding60d(우위), 각 {status,basis} 정적 매핑, PRD §4.4/US-6 원문 그대로.
  - src/lib/positionPlan.js(신규): `computePositionPlan({ticker,entryPrice,entryDate?},series)` —
    R-배수(현재수익%÷STOP_PCT%), 손절 참고 2종(체결가 기준 고정−8%/ATR비례, 검증상태 포함),
    브레이크이븐 알림(R≥2), 트레일링 참고(entryDate 있을 때만 — 보유중 최고종가×0.85,
    손절선과 max 비교해 강조), 이익 보호 알림(peak≥+20% AND 현재≤peak×50%).
    entry.js의 STOP_PCT/ATR_STOP_MULT, indicators.js의 atr() 재사용(재구현 없음).
  - src/lib/positionPlan.test.js(신규): 11개 — R-배수 계산·브레이크이븐 경계(R=2.0 정확/
    직하, AC1 3개), 이익보호 경계(peak20%+giveback50% 정확/직하/peak미달, AC1 3개),
    entryDate 유무 분기(미제공·트레일링계산·범위밖, AC2 3개), 검증상태(열위존재·출력에
    상태코드 실림, AC3 2개).
- 승인 기준 확인: AC1(6개 요구, 6개) 통과, AC2(분기 테스트) 통과, AC3(−8% 열위 존재 +
  출력에 상태코드 포함) 통과.
- 테스트 수 변화: 564 → 575 (+11). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-7 백테스트 국면 귀속 + 스키마 v3 골격 — backtest.mjs가 신호일마다
  regime.js를 슬라이스 기준으로 호출(미래참조 불가 유지), backtestSchema.js schemaVersion 3
  + regimeAxis[], v1/v2 로더 하위호환 유지.

## Iteration 7 — US-7. 백테스트 — 국면 귀속 + 스키마 v3 골격
- 완료 스토리: US-7
- 변경 파일:
  - src/lib/backtestSchema.js: SCHEMA_VERSION 2→3, SUPPORTED_SCHEMA_VERSIONS=[1,2,3],
    REGIME_VALUES=['up','neutral','down'], `regimeAxis[]` 검증(선택 필드 — freshnessCohorts와
    동일한 하위 호환 패턴: 없으면 v1/v2로 간주해 검증 생략).
  - src/lib/backtestSchema.test.js: 6개 추가 — schemaVersion3 허용·regimeAxis 없어도 통과·
    유효 항목 통과·regime enum 오류·strategyKey enum 오류·byHolding 수치 타입 오류.
  - scripts/backtest.mjs: `evaluateAsOf()`가 regime.js의 `currentRegime(dataset.tickers)`를
    슬라이스된(미래 데이터 없는) dataset 기준으로 호출해 반환에 `regime` 추가(재구현 없음 —
    시점 정합성은 기존 sliceUniverseAsOf가 이미 보장). `buildSignalRecords()`가
    regimeInfo(선택 4번째 인자)를 받아 모든 레코드에 `regime` 코드를 병기. 신규
    `buildRegimeAxis()`가 aggregatePerformance()를 재사용해 전략×sample×국면×보유기간
    표를 조립(allSignals 기준만 — top5는 국면별로 쪼개면 표본이 급격히 작아짐,
    freshnessCohorts와 동일 원칙). `formatRegimeReinterpretation()` 콘솔 출력 추가(main()에
    배선) — In/Out 각각의 국면 구성비 + 국면별 초과수익을 regimeAxis에서 직접 읽어 표시.
  - scripts/backtest.test.js: 기존 "schemaVersion 2로 발행" 단언을 3으로 갱신(의도된 스키마
    버전 상승에 따른 기대값 변경 — 회귀 아님, v9.1 US-1이 검증하려는 signalQuality 필드
    자체는 무영향). 신규 5개 — regimeAxis 발행·전략×sample당 국면 3종 전부 존재·AC2 합산
    정합성(국면별 신호수 합=allSignals·all 전략 신호수, 4키×2샘플×3보유일=24케이스)·AC1
    시점 정합성(raw를 미리 슬라이스해 넣어도 동일한 국면 — 미래 데이터 미개입 증명)·
    formatRegimeReinterpretation 출력 형식.
- 승인 기준 확인: AC1(시점 정합성) 통과, AC2(합산 정합성, 24케이스 전수 확인) 통과,
  AC3(v1·v2 로더 하위호환 — 기존 회귀 테스트 그대로 통과 + v3 무효 픽스처 3케이스 거부) 통과.
- 테스트 수 변화: 575 → 586 (+11: backtestSchema 6개, backtest.mjs 5개). npm run test/build
  둘 다 통과.
- 다음 스토리 메모: US-8 백테스트 진입 변형 4종(scripts/lib/entries.mjs) — entryPoint.js의
  computePivot()/derivedPrices() import(재구현 금지), 체결가=max(T,당일시가), 확인일
  변형(entry_pivot_confirm2)은 신선도 역전 가설의 직접 검증이므로 특히 정확히 구현.

## Iteration 8 — US-8. 백테스트 — 진입 변형 4종
- 완료 스토리: US-8
- **스키마 변경(선행 작업)**: `scripts/collect_data.py`의 build_series()에 `open`(시가) 필드
  신규 추가 — entry_pivot_trigger의 "체결가=max(T,당일 시가)" 계산에 필요한데, 기존
  수집 스키마엔 시가가 아예 없었다(H/L/C/V만). yfinance는 원래 Open 컬럼을 제공하므로
  `cols` 목록에 추가만 하면 됐다. 기존 지표(SMA/RSI/MACD 등)는 전부 close/high/low/volume만
  쓰므로 이 순수 추가가 기존 로직에 전혀 영향을 주지 않는다. entries.mjs는 `bar.open ?? bar.close`
  로 open 필드가 없는 구버전 픽스처(2y/3y 샘플 등, 재생성하지 않음)와 하위 호환한다.
- 변경 파일:
  - scripts/collect_data.py: build_series() cols에 "Open" 추가, 스키마 docstring 갱신.
  - scripts/lib/entries.mjs(신규): `ENTRY_VARIANTS`(entry_close/entry_pivot_trigger/
    entry_pivot_trigger_vol/entry_pivot_confirm2) + `aggregateEntryVariant()` — 전부
    entryPoint.js의 computePivot()/derivedPrices() 재사용(재구현 없음). 체결 판정(fillRate)은
    보유기간과 무관하게 1회만 계산, 보유기간별로 체결조건부(conditional)/기회비용포함
    (opportunity, 미체결=초과수익0) 두 벌 성과 산출. confirm2는 연속 카운트가 피벗 이하
    하루라도 나오면 리셋되는 상태기계로 구현.
  - scripts/lib/entries.test.js(신규): 11개 — entry_close 항상체결, 트리거 갭업체결가/
    트리거체결가/21일미체결(AC1 3개), 거래량조건 미충족·충족(AC1 2개), confirm2
    연속확인체결·리셋확인(체결일 인덱스로 직접 검증)·21일내 미확인(AC1 3개),
    entryPoint.js 재사용 확인(AC2 1개), 스키마 형태(fillRate+conditional/opportunity
    필드 완전성, AC3 1개).
  - src/lib/backtestSchema.js: `entryVariants[]` 검증(선택 필드, 하위 호환 패턴 동일).
  - src/lib/backtestSchema.test.js: 5개 추가 — 하위호환·유효항목·name누락·fillRate타입
    오류·byHolding 필드 누락.
  - scripts/backtest.mjs: outRecords의 trend·top5(변형 D와 동일 스코프)를
    Object.values(ENTRY_VARIANTS)로 순회해 `entryVariants[]` 생성, 반환에 추가.
  - scripts/backtest.test.js: 3개 추가 — 4종 전부 발행·entry_close fillRate=1(또는 표본0)·
    변형 대상 신호수 교차 검증.
- 승인 기준 확인: AC1(손계산 픽스처 6개 이상 → 8개) 통과, AC2(entryPoint.js 함수 재사용 —
  트리거 값이 computePivot+derivedPrices 계산값과 정확히 일치함을 직접 확인) 통과,
  AC3(fillRate·두 벌 성과 스키마 통과) 통과.
- 테스트 픽스처 관련 함정(리셋 로직 검증): confirm2의 "연속 2일" 요구가 진짜 연속인지(단순
  "21일 내 2번 이상 피벗 위"가 아닌지) 확인하려면 fillRate만 봐서는 구분이 안 된다(둘 다
  결국 체결은 되므로) — 체결 인덱스(fillIdx)를 직접 비교해 리셋이 없었다면 더 이른 날짜에
  체결됐을 것임을 증명하는 방식으로 테스트했다.
- 테스트 수 변화: 586 → 605 (+19: entries 11 + schema 5 + backtest 3). npm run test/build
  둘 다 통과. python 가드레일 유닛테스트(test_collect_data.py) 14개도 open 필드 추가 후
  재확인 통과.
- 다음 스토리 메모: US-9 백테스트 청산 변형 3종 + 조합 3종(scripts/lib/exits.mjs 확장) —
  exit_stop_atr/exit_sma50_break/exit_climax, X1·X4는 exitSignals.js import(재구현 금지),
  조합은 entries.mjs의 결과와 결합.

## Iteration 9 — US-9. 백테스트 — 청산 변형 3종 + 조합 3종
- 완료 스토리: US-9
- **인터페이스 변경(선행 리팩터, 비파괴적)**: exits.mjs의 `checkStop` 시그니처를
  `(entryClose,peakClose,close)` 3개 위치 인자 → `{entryClose,peak,close,series,entryIdx,idx}`
  단일 객체로 변경 — ATR·X1·X4 기반 규칙은 스칼라 3개만으로는 판정 불가(시리즈 전체 문맥
  필요)하기 때문. 기존 테스트는 walkExit()을 통해서만 checkStop을 호출해 왔어서(직접 호출
  없음) 이 변경이 안전했다(scripts/lib/exits.test.js 11개 그대로 통과 확인). `walkExit()`에
  `entryPrice` 선택 인자 추가(기본값=진입일 종가, 하위 호환) — US-9 combos가 진입 변형의
  체결가(신호일 종가와 다를 수 있음)를 넘길 수 있어야 했다.
- 변경 파일:
  - scripts/lib/exits.mjs: 신규 3종 — exit_stop_atr(체결가−2.5×ATR14 1회 산정 손절+60일,
    indicators.atr() 재사용)/exit_sma50_break(X1)/exit_climax(X4) — 후자 2종은
    `evaluateExitSignals(series.slice(0,idx+1))`를 매일 호출해 해당 코드의 트리거 여부만
    확인(exitSignals.js 재사용, 재구현 없음). `COMBOS`(3개: confirm2×atr, trigger_vol×sma50break,
    confirm2×sma50break) + `computeComboPerformance()`/`aggregateComboPerformance()` 신규 —
    entries.mjs의 진입 변형으로 체결한 뒤 그 체결가/체결일부터 청산 규칙으로 보유를
    시뮬레이션(진입·청산 두 모듈을 그대로 조합, 재구현 없음).
  - scripts/lib/exits.test.js: 10개 추가 — exit_stop_atr 도달/미도달(AC1 2개),
    exit_sma50_break 트리거/미트리거+exitSignals.js 직접 호출과 트리거일 일치 확인
    (AC1/AC2 3개), exit_climax 트리거/미트리거(AC1 2개), combos 3종 파라미터 명시·조합
    성과 필드 완전성·미체결 처리(AC3 3개).
  - src/lib/backtestSchema.js: `entryVariants[]`(US-8 마무리 누락분 없음, 이미 존재)에 이어
    `combos[]` 검증(선택 필드, adopted boolean 포함) 추가.
  - src/lib/backtestSchema.test.js: 4개 추가 — combos 하위호환·유효항목·adopted 타입오류·
    name누락.
  - scripts/backtest.mjs: `entryVariants`와 동일 스코프(trend·top5·Out)로 `combos[]` 조립,
    전부 adopted:false. 기존 `evaluateExitVariants()`는 `Object.values(EXIT_RULES)`를
    이미 제네릭하게 순회하므로 코드 변경 없이 신규 3종이 자동으로 variants[]에 편입됨.
  - scripts/backtest.test.js: 기존 "총 5종" 단언을 "총 8종"으로 갱신(의도된 확장 —
    EXIT_RULES에 3종 추가 + 제네릭 순회 로직 불변, 회귀 아님). 신규 2개 — variants[]에
    신규 3종 존재 확인, combos 3종 발행+전부 adopted:false 확인.
- 승인 기준 확인: AC1(신규 청산 각각 픽스처 손계산, 요구 6개 이상 → 7개) 통과,
  AC2(exitSignals.js 호출 확인 — 트리거일이 직접 호출 결과와 정확히 일치) 통과,
  AC3(조합 레코드에 진입·청산 양쪽 파라미터 명시 + adopted=false — COMBOS 정의 자체의
  entryVariant/exitRule 필드로 검증, adopted는 backtest.mjs 통합 레벨) 통과.
- 테스트 수 변화: 605 → 621 (+16: exits 10 + schema 4 + backtest 2). npm run test/build
  둘 다 통과. 전체 스위트 실행 시간이 늘어남(약 25→42초) — combos 시뮬레이션이 신호마다
  진입+청산 전체 경로를 다시 걷기 때문. 아직 허용 범위지만 US-10 이후 계속 늘면 참고할 것.
- 다음 스토리 메모: US-10 백테스트 상태별 분해 + 소프트 정책 변형 3종 — stateAxis[]는
  judgeEntryState()의 상태(0/1/2/3/산정불가) 재사용, actionable_only_top5는 상태1·2만
  top5 구성(v9.1 no_recent_breakout 코호트 열위 발견의 직접 검증).

## Iteration 10 — US-10. 백테스트 — 상태별 분해 + 소프트 정책 변형 3종
- 완료 스토리: US-10
- 변경 파일:
  - scripts/backtest.mjs: `buildSignalRecords()`의 trend/minervini/consensus 레코드 전부에
    `entryState`(0/1/2/3/'산정불가') 추가 — entryPoint.js의 judgeEntryState() 재사용
    (재구현 없음). `buildStateAxis()`(regimeAxis와 동일 패턴, aggregatePerformance 재사용,
    allSignals 기준만) + `buildPolicyVariants()`(이미 계산된 outRecords에 정책 필터만
    적용 — 재시뮬레이션 없음) 신규, 둘 다 runBacktest() 반환에 배선.
  - scripts/lib/variants.mjs: `rebuildTop5WithPolicy(poolRecords,predicate)`(날짜별로 묶어
    순위 정렬 후 predicate 통과 상위 5개 재구성 — predicate가 항상 true인 날짜는 원래
    top5와 수학적으로 동일해짐, 이 성질이 AC3의 근거) + `evaluatePolicyVariant()`(변형
    A/B/C의 `evaluateVariant()`와 반환 형태는 같지만 재시뮬레이션 없이 이미 있는 레코드만
    사용 — sampleThreshold를 정책마다 다르게 받음, PRD §7: 국면조건부=50/상태필터=100).
  - src/lib/backtestSchema.js: `stateAxis[]` 검증(선택 필드, state는 숫자0~3 또는
    '산정불가' 문자열 혼합 enum).
  - src/lib/backtestSchema.test.js: 3개 추가 — 하위호환·혼합타입 유효항목·state enum 오류.
  - scripts/backtest.test.js: 기존 "총 8종" 단언을 "총 11종"으로 갱신(정책 변형 3종 추가 —
    의도된 확장). 신규 8개 — stateAxis 발행·전략×sample당 상태5종 전부 존재·AC1
    완전성(4키×2샘플×3보유일=24케이스 전수, US-7/US-9와 동일 패턴)·정책 변형 3종 존재+
    adopted false, `rebuildTop5WithPolicy` 단위 테스트 3개(AC2: 상태0·3 제외 시 실제로
    top5 구성이 달라짐 — F·G가 승격되는 것까지 직접 검증, AC3: up 국면에서는 원래 top5와
    완전 동일 / down 국면에서는 완화 신호만 제외되고 다음 순위 승격).
- 승인 기준 확인: AC1(완전성·배타성, 24케이스 전수) 통과, AC2(actionable_only_top5가
  상태0·3을 실제로 제외하고 다른 순위가 승격되는 것을 티커 단위로 직접 확인) 통과,
  AC3(국면 조건부 변형이 다른 국면에서 불변임을 직접 확인 — up 국면 픽스처로 baseline과
  완전히 동일함을 증명) 통과.
- 설계 결정: 정책 변형은 변형 A/B/C처럼 evaluationDates를 처음부터 재시뮬레이션하지
  않는다 — 스코어링이 아니라 "이미 계산된 순위 목록의 구성 규칙"만 바꾸므로, 이미
  가진 outRecords(allSignals)에 필터+재정렬만 적용하는 편이 정확하고 훨씬 저렴하다.
  actionable_only_top5는 trend 전략에만 적용(진입/청산/조합 변형과 동일한 기존 스코프
  선례를 따름 — PRD가 대상 전략을 명시하지 않아 판단).
- 테스트 수 변화: 621 → 631 (+10: backtest 7 + schema 3). npm run test/build 둘 다 통과.
  전체 스위트 실행 시간 42→44초로 소폭 증가(정책 변형이 outRecords 전체를 날짜별로
  재그룹핑하는 추가 연산, combos 대비는 훨씬 저렴함).
- 다음 스토리 메모: US-11 백테스트 NGX 전체 프로토콜(scripts/backtest.mjs 유니버스
  경로화) — --data=/--out= 인자로 ngx100.json→backtest_ngx.json, RS 백분위·벤치마크가
  입력 유니버스 내부 기준으로 자동 계산됨을 하드코딩 검출 테스트로 확인, UI 로더는
  backtest_ngx.json을 참조하지 않아야 함(US-1에서 만든 ngx100.sample.json 픽스처 재사용).

## Iteration 11 — US-11. 백테스트 — NGX 전체 프로토콜
- 완료 스토리: US-11
- 변경 파일:
  - scripts/backtest.mjs: `runBacktest()`에 `universe='ndx'` 옵션 추가, config에
    `universe`+`universeStats`(tickerCount/hasFullYearDataExcludedCount/
    hasFullYearDataExcludedRatio, indicators.hasFullYearData() 재사용) 기록.
    `--universe=ndx|ngx` CLI 플래그(validateCliArgs가 검증), `--data=`(named, 기존
    위치인자와 공존 — flags.data 우선) 지원. universe별 기본 경로(ngx100.json→
    backtest_ngx.json, fundamentals_ngx.json) 분기. `formatNgxVsNdxComparison()` —
    기존 production backtest.json이 있으면 ★★(out·top5·20일) 나란히 비교 출력,
    없으면 생략(PRD §7 판정 기준표 재료, 실제 3항목 판정은 운영자 몫).
  - src/lib/backtestSchema.js: config.universe/universeStats 선택 필드 검증 추가.
  - scripts/backtest.test.js: 기존 validateCliArgs 정확 일치 단언 2건에 universe:'ndx'
    반영(의도된 확장). 신규 4개 — AC1(US-1의 ngx100.sample.json 3티커×756일 픽스처로
    완주+스키마 통과+universe 메타/제외율 기록), universe 기본값 ndx 확인, AC2(동일
    코드로 ndx/ngx 양쪽 실행 시 신호 티커가 절대 교차 오염되지 않음을 runSignalLoop
    직접 호출로 확인 — "하드코딩 없음"의 실증), AC3(backtestLoader.js 소스에
    'backtest_ngx' 문자열이 없고 'data/backtest.json' 리터럴만 있음을 직접 확인).
- 승인 기준 확인: AC1(NGX 픽스처 완주+메타 기록) 통과, AC2(하드코딩 없음 — 교차
  오염 부재 실증) 통과, AC3(UI 로더 미참조) 통과.
- 테스트 수 변화: 631 → 635 (+4). npm run test/build 둘 다 통과. 전체 스위트 44→52초
  (NGX 픽스처 실행 3건 추가).
- v10 US-1~11 완료 — 코드 스토리는 여기까지가 전부(US-12/13/14는 UI+localStorage 스토리).
- 다음 스토리 메모: US-12 localStorage 스키마 v4 — positions 필드, v3→v4 마이그레이션
  기존 필드 전부 보존, 사라진 티커 position 자동 제거, entryPrice 유효성(양수) 검증.

## Iteration 12 — US-12. localStorage 스키마 v4
- 완료 스토리: US-12
- 변경 파일:
  - src/lib/persistence.js: SCHEMA_VERSION 3→4, MIGRATABLE_FROM_VERSIONS=[1,2,3].
    DEFAULT_UI_STATE에 `positions:{}`(ticker→{entryPrice,entryDate?})와
    `expandedEntryEvidence:{}`(ticker→boolean, 화면2 US-13 진입가 근거 카드 펼침 상태)
    추가. `sanitizePositions()` — entryPrice가 유한 양수가 아니면 포지션 전체를 버림
    (부분 필드 무효화 아님 — 체결가 없는 포지션은 의미 없음), entryDate는 문자열이 아니면
    그 필드만 빠짐(포지션 자체는 유지). `sanitizeBooleanMap()` — 사라진 티커 제거 +
    boolean 아닌 값 제거, weights와 동일한 패턴.
  - src/lib/persistence.test.js: 기존 v1→v2/v2→v3 마이그레이션 테스트의 schemaVersion
    단언을 3→4로 갱신(의도된 확장) + 두 테스트 모두에 신규 v4 필드 기본값 확인 추가.
    신규 "v3 → v4 마이그레이션" describe 8개 — AC1(v3 전 필드 무손실 보존, 신규
    필드만 기본값), positions 왕복(entryPrice+entryDate 유효), AC2(사라진 티커 제거 /
    entryPrice 0·음수·문자열 각각 포지션 전체 무효화 / entryDate만 이상해도 entryPrice는
    보존 / expandedEntryEvidence 사라진티커·비boolean 제거), AC3(v4 필드 없는 기존
    저장분도 기본값으로 정상 로드).
- 승인 기준 확인: AC1(v3 무손실 마이그레이션, v8/v9 필드 포함) 통과, AC2(무효 position
  정리 + 사라진 티커 제거) 통과, AC3(기존 스토리지 테스트 전체 통과) 통과.
- 테스트 수 변화: 635 → 642 (+7). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-13 화면2 UI — Recommend.jsx에 국면 배지(regime.js)·진입 상태
  배지+가격세트(entryPoint.js)·매도 신호 배지(exitSignals.js)를 기존 카드 구조에 추가만
  (무변경 유지), backtest.json v3 regimeAxis 부재 시 국면 영역만 비표시.

## Iteration 13 — US-13. 화면2 — 국면 배지 · 진입 상태 · 매도 신호 · 검증 상태
- 완료 스토리: US-13
- 변경 파일:
  - src/components/RegimeBadge.jsx(신규): regime.js의 currentRegime() 결과 + backtest.json의
    regimeAxis(strategyKey='consensus_2star'·sample='out'·해당 regime)를 표시. **backtest나
    regimeAxis 자체가 없으면(v1~US-12 산출물) 영역 전체를 렌더링하지 않는다** — 국면 자체는
    dataset만으로 계산 가능하지만 검증된 Out 성과 맥락 없이 보여주는 건 "측정이 먼저" 원칙에
    어긋나 의도적으로 배지 표시 자체를 backtest.json v3 존재에 게이팅했다(BacktestConfidence
    컴포넌트의 기존 게이팅 관례와 동일선).
  - src/components/EntryPriceCard.jsx(신규): entryPoint.js의 judgeEntryState()/
    derivedPrices(), indicators.js의 atr() 재사용(재구현 없음). 상태별 배지+가격세트(상태0
    트리거+거리, 상태1 트리거+손절참고, 상태2 피벗+유효상단+손절참고2종+경과일+거래량배지,
    상태3 가격 미제시), 손절 참고가마다 constants/verification.js의 검증상태를 같은
    문단에 항상 동반(분리 렌더 불가), 산출 근거 펼침(expandedEntryEvidence, US-12 필드
    재사용), 기준일·목표가없음·진입가 면책 고정 문구.
  - src/components/ExitSignalBadge.jsx(신규): exitSignals.js의 evaluateExitSignals() 재사용
    (재구현 없음). rsPercentileValue는 화면2 맥락에 없어 null 전달(X3만 계산 대상 제외,
    X1/X2/X4/X5는 정상). "매도 신호 없음" 또는 "⚠ n건" + 펼침 상세.
  - src/screens/Recommend.jsx: RegimeBadge를 최상단에 1회, EntryPriceCard/ExitSignalBadge를
    SelectAndResearch(미너비니·컨센서스 공용) + TrendModeView 인라인 카드 양쪽에 배선.
    tickerDataMap/generatedAt/expandedEntryEvidence/onToggleEntryEvidence를 3개 모드뷰에
    전부 스레딩 — 기존 마크업·로직은 무수정(추가만).
  - src/App.jsx: `tickerDataMap`(dataset.tickers dataSufficient만, ticker→원본 데이터),
    `regimeInfo`(currentRegime(dataset.tickers), 클라이언트에서 바로 계산 — 백엔드 호출
    없음), `toggleEntryEvidence` 신규, Recommend에 4개 prop 추가 배선.
  - 신규 테스트 24개: RegimeBadge(10 — backtest/regimeAxis/regime null 3종 미표시(AC4),
    국면 3종 렌더링(AC1), 표본충족/부족 문구, regimeAxis 항목 자체 없음, 고정 고지),
    EntryPriceCard(11 — tickerData 없음 미표시(AC4), 산정불가·상태0~3 렌더링(상태3
    가격 미제시 확인, AC2), earlyBreakout 문구, 검증열위 라벨 동반(상태1·2 둘 다, AC3),
    펼침 토글, 기준일/면책 고지), ExitSignalBadge(3 — 미표시·신호없음·신호트리거+펼침).
- 승인 기준 확인: AC1(국면 배지 3상태+표본부족+고지 동반) 통과, AC2(진입 상태 4종+상태3
  가격 미제시+earlyBreakout) 통과, AC3(검증 열위 라벨이 같은 문단에 항상 동반 — 텍스트
  분리 불가를 실제로 확인) 통과, AC4(기존 App.test.jsx 13개+Recommend.test.jsx 30개
  전부 무수정 그대로 통과 — 신규 컴포넌트가 순수 추가임을 증명) 통과.
- 검증: 기존 43개(App 13 + Recommend 30) 테스트가 이 세션에서 단 한 줄도 수정 없이
  그대로 통과 — 진짜 "추가만" 구현되었다는 강한 증거. 브라우저/스크린샷 도구가 이
  프로젝트에 없어(기존에 확인된 제약) jsdom 렌더 테스트가 실질적 시각 검증 수단.
- 테스트 수 변화: 642 → 666 (+24). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-14 화면3·4 청산계획 패널 + 문서 마감 — Simulation/Portfolio에
  positionPlan.js(US-6) 연동(체결가 입력→R배수/손절/트레일링/이익보호), 매도신호배지
  병기, README 분기 재검증 절차 + PRD §7 판정기준표 전재, PRD §8 성공기준 8항목 점검.

## Iteration 14 — US-14. 화면 3·4 — 청산 계획 패널 + 분기 재검증 문서 + 마감 (v10 마감)
- 완료 스토리: US-14 — **v10 US-1~14 전체 완료**
- 변경 파일:
  - src/components/PositionPlanPanel.jsx(신규): positionPlan.js의 computePositionPlan()
    재사용(재구현 없음). 체결가(필수)·체결일(선택) 입력 필드 + R-배수·손절참고2종
    (검증상태 동반)·브레이크이븐 알림·트레일링 참고·이익보호 알림 + ExitSignalBadge
    (US-13 컴포넌트 재사용) 병기 + 예약주문 안내·청산 면책 고정 문구. 체결가 미입력
    시 계획 산출부 없이 입력 필드만 표시.
  - src/screens/Simulation.jsx / Portfolio.jsx: PositionPlanPanel을 각 티커 카드에
    배선(Portfolio는 한 줄 요약 행 아래로 패널이 들어가도록 컨테이너만 재구조화,
    기존 마크업·로직 무수정). `positions`/`onChangeEntryPrice`/`onChangeEntryDate`
    prop 추가.
  - src/App.jsx: `setPositionEntryPrice`/`setPositionEntryDate` 신규 — 체결가가
    비거나 무효(0 이하)면 포지션 자체를 삭제(persistence.js의 sanitizePositions와
    동일 원칙), 체결가 없이 체결일만 입력 불가. Simulation/Portfolio 양쪽에 배선.
  - src/components/PositionPlanPanel.test.jsx(신규): 8개 — tickerData 없음/체결가
    없음 시 입력필드만(2), R-배수+검증상태 동반, 브레이크이븐(+2R), 체결일 미입력 시
    "체결일 입력 시 제공", 체결일 입력 시 트레일링 계산, 입력 변경 콜백 호출,
    고정 안내 문구 2종.
  - README.md: "v10 분기 재검증 절차" 신규 절 — 양 유니버스 재수집(QQQJ CSV 갱신
    포함)·양 유니버스 백테스트 재실행·표시 자동 갱신 확인·PRD §7 판정 기준표 전재·
    progress 기록의 5단계. "PRD_Nasdaq10 §8 성공 기준 점검" 신규 절 — 8개 항목 전부
    [x], NGX 3년 실데이터 수집 항목만 "코드 완비, 실행은 운영자 몫" 각주.
- 승인 기준 확인: 체결가 입력→계획 산출→localStorage v4 왕복(US-12에서 이미 검증된
  positions 필드를 그대로 재사용) + entryDate 생략 분기(테스트로 확인), 알림 배지
  조건 렌더링(+2R 테스트로 확인), README 절차·판정 기준표 존재 + 성공 기준 점검
  기록 + 전체 테스트·빌드 통과 전부 확인.
- 테스트 수 변화: 666 → 674 (+8). npm run test/build 둘 다 통과.
- **v10 전체 요약**: US-1~14 전 스토리 코드 구현 완료, 14개 커밋 전부 로컬에 존재(푸시는
  별도 확인 필요). 테스트 수 503(v9 종료 시점) → 674(+171, 신규 lib 8개 + 백테스트
  확장 5개 파일 + UI 컴포넌트 6개). 운영자 액션으로 남은 것: (1) NGX 실데이터 수집
  (QQQJ CSV 확보 필요), (2) 양 유니버스 backtest.json 실행, (3) §7 판정 기준표 대조로
  NGX 노출·진입/청산 변형·정책 채택 여부 결정 — 전부 README "v10 분기 재검증 절차"에
  명문화됨. 코드 커밋을 푸시할지, 이 세션에서 실데이터 액션까지 이어서 할지는 사용자
  확인 필요.

# v11 Ralph 루프 진행 기록 (prd-v11-pullback_1.md) — 실제 Ralph 루프 via /loop 스킬 (사용자 선택)

- v10 전부 푸시·배포 완료 확인 후, 사용자가 NGX 실데이터 수집(SEC EDGAR NPORT-P + OpenFIGI
  ISIN→티커 매핑으로 QQQJ 106종목 확보) + 양 유니버스 백테스트 재실행을 요청 → 완료·커밋·
  푸시·배포 검증까지 완료(별도 데이터 커밋). 이어서 v10 §7 판정 기준표 대조 요청 →
  전부 미충족/보류로 판정(단, US-10 정책변형의 "국면별 표본" 판정 방식에 실제 버그 발견 —
  전체기간 표본을 썼어야 할 국면별 표본 대신 사용). 그 직후 사용자가 PRD_Nasdaq11.md +
  prd-v11-pullback_1.md(US-1~13) + project-summary-tables_2.md를 프로젝트 폴더에 추가.
  project-summary-tables_2.md가 인용하는 핵심 수치(상태0 교집합 +36~40%p 등)가 v10 §7
  판정에서 직접 계산한 stateAxis 원시 수치와 달라(원시: 평균+0.89%p·중앙값−5.33%) 실제
  신호 재현 루프에 템플릿통과+피벗할인(-10~-25%) 교집합 필터를 겹쳐 재현 시도 →
  미너비니 경로는 소수점까지 일치(+39.83%p/84.4%), 추세추종 경로도 방향성 일치(+28.22%p/
  78.6%, 점수상위 조건 제외한 근사치) — **전제 확인됨, 조작/환각 아님**. 사용자가 진행
  방식으로 Ralph 루프(/loop) 선택(v10은 직접 순차) — 세션마다 고정 선호 없음, 매번 확인.

## Iteration 1 — US-1. 데이터 수집 기점 2021-01-01
- 완료 스토리: US-1
- 변경 파일:
  - scripts/collect_data.py: `PERIOD="3y"`(period= 인자) → `START_DATE="2021-01-01"`
    (start= 인자)로 전환 — "기간 길이" 방식에서 "고정 기점" 방식으로 근본 전환. 근거
    주석(2022 약세장 포함 필요 + 10년 소급은 생존 편향 체증으로 보류) + 한계 고지
    로그 라인 추가. `download_batch()`는 NDX/NGX 양쪽 경로가 공유하므로 코드 변경
    없이 양쪽 다 새 기점을 자동 적용받음(US-1 승인 기준 1 "--universe 양쪽에서 동작").
    상단 모듈 docstring도 "3년 수집" → "고정 기점(2021-01-01) 수집"으로 갱신.
  - scripts/test_collect_data.py: 2개 추가 — START_DATE 상수 값 확인, download_batch
    소스 코드 텍스트 검사로 `start=START_DATE` 사용 + `period=` 미사용 확인(회귀 방지 —
    런타임 목킹보다 소스 텍스트 검사가 이 케이스엔 더 강함).
  - src/lib/__fixtures__/nasdaq100.5y.sample.json(신규): 6티커×1400거래일(약 5.5년)
    합성 픽스처 — 상승 600일(누적 +40%대) → 하락 300일(−45%, 진짜 약세장) → 회복
    500일(저점 대비 2배) 결정적 생성(sine+trend, Math.random 없음). 국면 분해 시
    up 855일·down 344일·neutral 2일·null(워밍업) 199일로 확인 — v10에서 하락 국면
    표본이 30건에 불과했던 한계를 이 픽스처가 해소한다.
  - src/lib/regime.test.js: 2개 추가 — 5.5년 픽스처에서 regimeSeries()가 up/down
    양쪽을 실제로 판정(AC2), 하락 국면 일수가 50일을 초과(v10 표본 부족 한계 해소 확인).
  - scripts/backtest.test.js: 2개 추가 — 5.5y 픽스처에서 runBacktest() 완주+스키마
    통과(AC3, 완주 21초 소요라 개별 테스트 타임아웃 30초로 상향), 5y 평가일 수가
    2y보다 규칙대로 많음(자동 적응 회귀 확인).
- 승인 기준 확인: AC1(기점 상수+근거주석+양쪽 유니버스 동작) 통과, AC2(5.5년 픽스처
  스키마 통과+국면 양쪽 판정) 통과, AC3(백테스트 완주+평가일 수 증가) 통과.
- 테스트 수 변화: 674(JS, v10 종료) → 678(JS, +4) / Python 14→16(+2, npm 카운트
  미포함, 별도 게이트). npm run test/build 둘 다 통과. 전체 스위트 실행시간 54→80초로
  증가(5.5y 백테스트 완주 테스트가 21초 소요) — 다음 스토리에서도 참고할 것(US-4의
  schemaVersion 4 골격 테스트도 이 픽스처를 재사용할 가능성 높음, 비용 누적 주의).
- 다음 스토리 메모: US-2 보유 지평 90/120 확장(청산 F) — performance.mjs의
  holdingDays 리스트 확장 + evaluationDates의 holdingBufferDays 60→120, overlapFactor
  계산에 90/120 추가, 기존 5/20/60 결과 불변 회귀 확인 필수.

## Iteration 2 — US-2. 보유 지평 90/120 확장 (청산 F)
- 완료 스토리: US-2
- 변경 파일:
  - scripts/backtest.mjs: `HOLDING_DAYS` [5,20,60] → [5,20,60,90,120], `runBacktest()`의
    `holdingBufferDays` 기본값 60→120. `computeOverlapFactor()`/`aggregatePerformance()`/
    backtestSchema.js의 byHolding 검증은 이미 holdingDays를 제네릭하게 순회하므로 코드
    변경 없이 90/120을 자동으로 반영 — 실제로 손댄 곳은 두 상수뿐인 저비용 스토리였다
    (PRD가 예고한 그대로). exits.mjs의 MAX_HOLDING_DAYS=60(청산 규칙 walk 시뮬레이션
    전용)과 variants.mjs의 COMPARISON_HOLDING_DAYS=20(변형 판정 비교 기준)은 이 스토리의
    "고정 보유 지평 벤치마크"와 무관한 별개 개념이라 그대로 둠.
  - scripts/backtest.test.js: 4개 추가 — 5개 지평 전부 byHolding/overlapFactor에 반영
    확인(5.5y 픽스처, US-1 픽스처 재사용), 말단여유 120이 60보다 평가일 수가 같거나
    적음(구조적 확인), AC1 경계(60일은 있고 120일은 없는 신호가 딱 그 지평에서만
    null — 90일치 데이터만 있는 합성 티커로 computeSignalPerformance 직접 호출),
    AC3 회귀 없음(aggregatePerformance에 90/120을 holdingDays 목록에 추가해도 90/120
    없이 호출했을 때와 5/20/60 그룹 결과가 완전히 동일 — 보유기간 그룹 간 교차 오염
    없음을 직접 증명). 기존 v9.1 stepDays 테스트의 overlapFactor 정확값 단언 1건을
    5개 지평 포함하도록 갱신(의도된 확장, 회귀 아님).
- 승인 기준 확인: AC1(말단여유 확대로 제외되는 신호가 해당 지평에서만 제외) 통과,
  AC2(overlapFactor 산출 + 5개 지평 스키마 검증) 통과, AC3(기존 5/20/60 결과 불변,
  그룹간 교차오염 없음을 직접 대조로 증명) 통과.
- 테스트 수 변화: 678 → 682 (+4). npm run test/build 둘 다 통과. 전체 스위트
  76~80초대 유지(holdingBufferDays 확대로 evalDates가 줄어 US-1의 5.5y 완주 테스트가
  오히려 소폭 빨라짐 — 21초→17초, 평가일 수 감소 효과가 5개 지평 추가 연산량 증가를
  상쇄).
- 다음 스토리 메모: US-3 부분 포지션 인프라(scripts/lib/performance.mjs 확장) — 청산
  이벤트 배열[{일자,비율,체결가}] 지원, 포지션 수익률=Σ(비율×구간수익률), 벤치마크도
  동일 가중 구조, 기존 전량청산 경로(비율=1 단일 이벤트로 취급) 결과 불변 회귀 확인.

## Iteration 3 — US-3. 부분 포지션 인프라
- 완료 스토리: US-3
- 변경 파일:
  - scripts/lib/performance.mjs: `computePartialPositionPerformance(record, priceIndex,
    exitEvents, finalHoldingDays)` 신규 — 기존 `computeSignalPerformance()`는 전량청산
    전용으로 완전히 그대로 두고(회귀 없음) 별개 함수로 추가. exitEvents=[{date,ratio}]의
    비율 합이 1 미만이면 잔여분을 finalHoldingDays 시점에 청산하는 레그로 자동 추가.
    **레그마다 보유기간이 다르므로 벤치마크도 레그별로 개별 계산**(universeBenchmarkReturn
    을 레그 보유기간만큼씩 따로 호출) 후 비중 가중합 — 하나의 벤치마크로 뭉뚱그리면
    정합성이 깨진다는 점을 명시. MDD는 "잔여 포지션 가중 곡선"(이미 청산된 레그는 그
    시점 수익률로 고정, 미청산 비중만 그날그날 종가로 계속 평가) 기준으로 기존
    maxDrawdown()을 그대로 재사용해 계산. 비율 합이 1 초과·청산일이 캘린더에 없음·
    잔여분 만기청산일이 데이터 범위 밖인 경우 전부 null.
  - scripts/lib/performance.test.js: 8개 추가 — 50%중도+50%만기 가중 수익률/벤치마크
    손계산 일치(AC1), 100%중도청산이 동일 보유기간 전량청산과 일치, **0%중도청산(이벤트
    없음)이 기존 computeSignalPerformance와 완전히 동일**(AC3 회귀 없음의 직접 증명),
    legs 배열 상세 기록 확인, 비율합=1 정확 경계(레그 2개, 잔여 없음), 비율합>1 무효
    입력 거부(AC2 "비율 합=1 검증"), 레그별 벤치마크가 실제로 다름을 먼저 확인한 뒤
    가중합 정합성 검증, 조기 절반청산이 후반 급락 노출을 줄여 MDD가 전량보유 이하임을
    V자 하락 픽스처로 확인, 청산일 캘린더 부재/잔여분 만기 범위초과 각각 null.
- 승인 기준 확인: AC1(손계산 픽스처 6개 이상 → 8개) 통과, AC2(가중 벤치마크·초과수익
  정합성 + 비율합 검증) 통과, AC3(기존 전량청산 경로 결과 불변 — 별도 함수로 추가했으므로
  구조적으로 보장 + 0%중도청산 케이스로 수치까지 동일함을 직접 증명) 통과.
- 테스트 수 변화: 682 → 692 (+10, performance.test.js 13→21 신규 8개 반영 확인 — 전체
  스위트 카운트 변화가 새 테스트 수와 정확히 일치). npm run test/build 둘 다 통과.
- 다음 스토리 메모: US-4 schemaVersion 4 골격 + 집계 축 2종 — stateRegimeAxis[](진입
  상태×국면×전략×sample, US-10 stateAxis/US-7 regimeAxis 조합 패턴 재사용), entryVariants에
  strategyKey 축 추가(모드별 분해), 검증기 v4 + 로더 v1~v3 하위호환, v1~v3 회귀 테스트

## Iteration 4 — US-4. schemaVersion 4 골격 + 집계 축 2종
- 완료 스토리: US-4
- 변경 파일:
  - scripts/backtest.mjs: `ENTRY_VARIANT_MODE_POOLS = ['trend', 'minervini', 'consensus_2star']`
    신규 상수. `entryVariants` 계산을 이 3개 풀에 대해 `.flatMap()`으로 재구성 — 각 풀은
    자기 자신의 `outRecords.filter(strategyKey===pool && basis==='top5')`만으로 독립 집계
    하고(풀 간 신호 누출 없음), 결과마다 `strategyKey`를 태그한다. 신규 함수
    `buildStateRegimeAxis(inRecords, outRecords, priceIndex, holdingDays)` 추가 — stateAxis
    (US-10)/regimeAxis(US-7)와 동일 패턴으로 state×regime 2차원 셀을 순회하며
    `aggregatePerformance()`를 재사용(재구현 없음). `runBacktest()` 반환 객체에
    `schemaVersion: 4`, `stateRegimeAxis` 추가. 신규 `formatState0RegimeTable(backtest)` —
    trend·Out·20거래일 기준 상태0×국면 3행 콘솔 표(v10 발견 #6 "구조 몫 vs 국면 몫" 분리
    재료), `main()`에 출력 라인 추가.
  - src/lib/backtestSchema.js: `SCHEMA_VERSION` 3→4, `SUPPORTED_SCHEMA_VERSIONS`에 4 추가.
    `validateEntryVariant`에 선택 필드 `strategyKey` enum 검증(STRATEGY_KEYS) 추가 — 없으면
    (v1~v3 산출물) 검증 생략. 신규 `validateStateRegimeAxisItem` — stateAxis/regimeAxis
    검증기를 그대로 조합(strategyKey/sample/state/regime enum + byHolding 재사용),
    `validateBacktest()`에 선택 필드로 배선.
  - scripts/backtest.test.js: 기존 schemaVersion 3 단언 → 4로 교체(주석 추가, v9.1 US-1
    describe). 기존 entryVariants "4종" 단언을 trend 풀로 좁힘(`trendVariants()` 헬퍼 추가,
    회귀 없음 — 다른 두 풀이 추가됐을 뿐). 신규 describe "v11 US-4" 3개 테스트: AC1
    stateRegimeAxis (state,regime) 셀 합 = stateAxis 합(4키×2sample×3days=24케이스 전수
    확인), AC2 각 모드 풀 entryVariants.signals가 자기 풀 own top5 표본 유무와만 대응함을
    확인(다른 풀 신호 섞임 없음의 근사 증거).
  - src/lib/backtestSchema.test.js: 신규 describe "stateRegimeAxis + entryVariants.
    strategyKey" 7개 테스트 — v1/v3 하위 호환 2개, 유효 stateRegimeAxis 통과 1개, 무효
    픽스처 거부 3케이스(state/regime/strategyKey enum 각각, AC3 "3케이스 이상" 충족),
    entryVariants.strategyKey 선택 필드 통과+enum 거부 1개. (v2는 베이스 픽스처가 v1이라
    signalQuality 필수 조건과 충돌해 규모 조정 — regimeAxis 등 기존 describe들과 동일한
    이유로 v3만 테스트.)
- 승인 기준 확인: AC1(stateRegimeAxis 합산 정합성) 통과, AC2(모드별 entryVariants 독립
  계산·풀 간 누출 없음) 통과, AC3(v1~v3 로더 하위호환 + v4 무효 픽스처 거부 3케이스 이상)
  통과 — schemaVersion.test.js에서 정확히 3케이스(state/regime/strategyKey enum) 확보.
- 테스트 수 변화: 692 → 702 (+10: backtest.test.js 74→77 신규 3개 + backtestSchema.test.js
  33→40 신규 7개). npm run test(전체 53개 파일, 702개) + npm run build 둘 다 통과.
- 다음 스토리 메모: US-5 눈림목 판정 lib(`src/lib/pullback.js` 신규) — PRD §4.5 눈림목
  정의(추세 상단 이탈 후 지지선 근접 재접근) 참고. schemaVersion 4의 stateRegimeAxis가
  이미 확인한 "상태0×down국면" 셀이 v10 발견 #6의 근거이므로, US-5는 그 상태0 신호를
  세분화하는 판정 함수가 될 가능성이 높음 — US-6(진입 변형 3종 + pullbackAxis)이 이
  lib을 바로 소비하도록 순수 함수·부작용 없음으로 설계할 것.
  필수(entryVariants 스키마 변경이 US-8 산출물 깨지 않는지 특히 주의).

## Iteration 5 — US-5. 눌림목 판정 lib
- 완료 스토리: US-5
- 변경 파일:
  - src/lib/constants/pullback.js: 신규 — PULLBACK_DEPTH_MIN_PCT(10)/MAX_PCT(25),
    PULLBACK_P3_SMA_PERIOD(200, SMA50 엄격형은 예비 상수만 보관·미적용),
    PULLBACK_TRIGGER_WINDOW_DAYS(10), PULLBACK_OBSERVATION_VALID_DAYS(30),
    PULLBACK_STOP_MULT(0.98), PULLBACK_RESUME_VOL_MULT(1.5, US-6 예비).
  - src/lib/pullback.js: 신규 `judgePullback(series, {rsPercentileValue})`. P1은
    `evaluateTrendTemplate()`(minervini.js) 직접 호출, P2 거리 계산은 `computePivot()`
    (entryPoint.js)의 pivot 값을 그대로 재사용(재구현 없음 — pivot 재계산 코드 없음).
    P3은 SMA200(indicators.sma 재사용) 비교. P4는 피벗 형성일(peakIndex — PH 산정과
    동일한 PIVOT_LOOKBACK 창에서 실제 최고 종가가 찍힌 날을 역산)을 기준으로 "피벗 이후
    눌림 구간"과 "피벗 이전 동일 길이 직전 상승 구간"의 평균 거래량을 비교(고갈 판정).
    파생값 3종: 눌림 저점(피벗 이후 최저 종가) · 재개 트리거가(직전 10거래일 최고 종가,
    오늘 포함) · 구조 손절 참고가(눌림 저점×0.98). 데이터 부족(63거래일 미만) 또는 피벗
    무효 시 insufficientData:true로 조기 반환.
  - src/lib/pullback.test.js: 신규 20개 테스트. 데이터 부족 1개, P2 경계 4개(−10%/−9.99%/
    −25%/−25.01%), P3 경계 2개(SMA200 위/아래 — "다른 199일 평균과 같아지는 지점"으로
    경계값을 손계산해 도출), P4 4개(고갈 충족/미충족/거래량 동일 시 미충족/직전 구간
    데이터 부족 시 미충족), AC2 재사용 확인 1개(checks.P1 === evaluateTrendTemplate().
    allPassed, pivot === computePivot().pivot 정확히 일치), 파생 가격 손계산 4개(눌림
    저점이 마지막날 종가가 아닌 구간 전체 최저임을 확인하는 픽스처 포함), 통합
    observed:true 시나리오 2개("낮은 베이스 150일 + 급상승 110일 + −15% 눌림 5일"로
    P1~P4를 모두 동시에 충족시키는 픽스처 — 스크래치 스크립트로 여러 decline
    기간·depth 조합을 사전 스윕해 실제로 전 조건이 동시 충족되는 지점을 확인한 뒤
    채택), missingConditions 1개, 상수 노출 1개.
- 승인 기준 확인: AC1(P1~P4 경계 테스트 8개 이상 → 10개: P2 4 + P3 2 + P4 4) 통과,
  AC2(피벗·템플릿이 기존 lib 실호출임을 값 일치로 증명) 통과, AC3(손계산 4개 이상 →
  4개: 눌림저점/트리거가/손절가/peakDate) 통과.
- 테스트 수 변화: 702 → 722 (+20, pullback.test.js 신규 20개 — 전체 스위트 카운트 변화가
  정확히 일치). npm run test(전체 54개 파일, 722개) + npm run build 둘 다 통과.
- 다음 스토리 메모: US-6 눌림목 진입 변형 3종(pullback_immediate/resume/resume_vol) +
  pullbackAxis — scripts/lib/entries.mjs 확장, pullback.js의 judgePullback()을 import해
  재구현 없이 그대로 소비할 것. resume/resume_vol은 관찰 성립일로부터
  PULLBACK_OBSERVATION_VALID_DAYS(30거래일) 내 트리거가 돌파 여부를 봐야 하므로, 신호
  레코드 생성 시점에 그날의 judgePullback() 결과(트리거가·관찰 성립 여부)를 어떻게
  entries.mjs 쪽 신호 루프에 연결할지가 US-6의 핵심 설계 지점(US-8 진입변형처럼
  ENTRY_VARIANTS 객체의 simulate(series, entryIdx) 시그니처와 호환되게 만들 것 — series
  기반으로 매 신호일마다 judgePullback을 그 시점 슬라이스에서 재호출하는 형태가 유력).
  pullbackAxis는 국면 분해가 필수(PRD §4.2) — buildRegimeAxis/buildStateRegimeAxis와
  동일 패턴(aggregatePerformance 재사용)으로 구현.

## Iteration 6 — US-6. 눌림목 진입 변형 3종 + pullbackAxis
- 완료 스토리: US-6
- 예상 밖 발견 및 아키텍처 보강(구현 중 필수로 드러남): P1(트렌드 템플릿)의 T8(RS 백분위)은
  유니버스 전체를 봐야 계산되는 값이라, 개별 티커 series만 받는 entries.mjs의 simulate()
  컨텍스트에서는 원래 얻을 수 없었다(exits.mjs의 X3도 동일한 한계로 rsPercentileValue=null
  고정 — 기존에 이미 있던, 조용히 넘어가던 제약). 그대로 두면 P1이 항상 false가 되어
  pullback_immediate/resume/resume_vol이 실제 데이터에서 영원히 0건이 되는 구조적 결함이라,
  단순 accept 대신 근본 수정: `scripts/backtest.mjs`의 `buildSignalRecords()`가 매 평가일
  한 번 `buildRsPercentileMap(datasetTickers)`(rsRawScore/rsPercentile/hasFullYearData —
  전부 minervini.js가 이미 쓰는 기존 함수 재호출, 재구현 없음)로 그 날짜의 RS 백분위 맵을
  만들어 trend/minervini/consensus 레코드 전부에 `rsPercentileValue`를 공통 부여하도록
  확장(entryState/freshnessCohort와 동일한 "평가일당 1회 계산, 전 레코드 공유" 패턴). 이어
  `aggregateEntryVariant()`가 `variant.simulate(series, idx, r.rsPercentileValue)`로 3번째
  인자를 넘기도록 확장(기존 ENTRY_VARIANTS 4종은 인자를 그냥 무시하므로 회귀 없음).
- 변경 파일:
  - scripts/lib/entries.mjs: `PULLBACK_ENTRY_VARIANTS`(pullback_immediate/resume/resume_vol)
    신규 export — 별도 맵으로 두어 기존 ENTRY_VARIANTS(entryVariants[] 모드별 4종, US-4/US-8)
    구성은 전혀 건드리지 않음. `simulatePullbackImmediate`/`simulatePullbackResume`은 신호일
    까지 슬라이스(asOfSeries)로 `judgePullback()`(pullback.js, 재구현 없음)을 호출해 관찰
    조건을 판정하고, resume 계열은 그 결과의 `triggerPrice`를 그대로 재사용해 30거래일 내
    재개(종가>트리거, 체결가=max(트리거,시가) — 기존 entry_pivot_trigger와 동일한
    fillPriceForBar 재사용)를 탐색. `aggregateEntryVariant()`에 rsPercentileValue 전달 배선.
  - scripts/backtest.mjs: `buildRsPercentileMap()` 신규 + `buildSignalRecords()`가 매
    레코드에 `rsPercentileValue` 부여. `PULLBACK_ENTRY_VARIANTS` import, 신규
    `buildPullbackAxis()`(trend·basis·국면 필터 조합마다 aggregateEntryVariant 재사용,
    adopted 항상 false), `runBacktest()` 반환에 `pullbackAxis` 추가(schemaVersion은 4 유지 —
    US-4 이후 신규 선택 필드 추가가 항상 버전을 올리진 않는 기존 관례 그대로). 콘솔에
    `formatPullbackComparison()`(3종×국면, v10 상태0 실측 대조 주석 포함) 추가.
  - src/lib/backtestSchema.js: `PULLBACK_VARIANT_NAMES` 상수 + `validatePullbackAxisItem`
    신규, `validateBacktest()`에 선택 필드로 배선(하위 호환 — 없으면 검증 생략).
  - scripts/lib/entries.test.js: 신규 8개 테스트 — pullback_immediate 관찰충족/미충족 각 1,
    AC1 손계산 6개(재개 미발생, 관찰 자체 미충족, 갭업 체결가=시가, 정상개장 체결가=트리거,
    거래량 미달로 resume만 체결, 거래량 충족 시 resume_vol도 체결), AC2 신호집합 포함관계
    (4개 티커 시나리오로 resume_vol(1)⊆resume(2)⊆immediate(3) 직접 확인). **발견한 테스트
    픽스처 버그**: entries.test.js의 `makeBar()`는 high를 close로 자동 대체하지 않는데
    (다른 기존 진입변형 픽스처는 항상 high를 명시해 와서 드러나지 않았던 잠재 결함),
    새 픽스처가 high 없이 close만 넘기면서 week52HighLow() 기반 T6/T7이 깨져 P1이 항상
    false로 나오는 문제를 겪음 — `observedPullbackBaseBars()`에서 high/low를 close로
    명시하도록 수정해 해결(재현 가능한 스크래치 스크립트로 원인 격리 후 수정, 실제
    pullback.js/entries.mjs 로직 버그가 아니었음을 확인).
  - scripts/backtest.test.js: `aggregateEntryVariant, PULLBACK_ENTRY_VARIANTS` import 추가.
    신규 describe "v11 US-6" 3개 테스트: pullbackAxis 3×2×2×3=36셀 전수 존재 확인, AC3
    국면분해 합산 정합성(runSignalLoop로 직접 재구성한 out·trend·top5 레코드를 국면 무필터로
    재집계한 값과 pullbackAxis 3국면 합이 정확히 일치 — **buildEvaluationDates()의 자체
    기본값(holdingBufferDays=60)이 runBacktest()의 기본값(120)과 달라 처음엔 불일치가
    났고, 옵션을 명시적으로 맞춰 해결**), adopted 전부 false 확인.
  - src/lib/backtestSchema.test.js: 신규 describe "pullbackAxis" 6개 테스트 — 하위 호환,
    유효 항목 통과, name/regime enum 거부, adopted 타입 거부, byHolding 필드 누락 거부.
- 승인 기준 확인: AC1(손계산 픽스처 6개 이상 → 6개) 통과, AC2(3종 신호 집합 관계
  resume_vol⊆resume⊆immediate) 통과, AC3(국면 분해 합산 정합성 + adopted 전부 false) 통과.
- 테스트 수 변화: 722 → 740 (+18: entries.test.js +8, backtest.test.js +3,
  backtestSchema.test.js +6, 그리고 기존 US-8 entryVariants 단언 2건을 trend 풀 한정으로
  좁힌 것은 US-4에서 이미 반영되어 있었음/신규 아님 — 순수 신규 테스트 카운트는 8+3+6=17에
  근접, 스위트 전체 카운트 변화(+18)로 최종 확인). npm run test(전체 54개 파일, 740개) +
  npm run build 둘 다 통과.
- 다음 스토리 메모: US-7 청산 변형 A(국면 조건부 + regime-flip) — scripts/lib/exits.mjs 확장.
  `exit_regime_conditional`: 신호일 국면 상승→60일 보유 / 중립·하락→ATR손절(2.5×ATR14).
  US-6에서 확립한 "rsPercentileValue를 레코드에 미리 부여" 패턴처럼, 국면도 이미 레코드에
  `regime` 필드로 붙어 있으므로(v10 US-7) 추가 배선 없이 바로 재사용 가능 — exits.mjs가
  이미 하듯 EXIT_RULES 객체에 새 규칙만 추가하면 됨. regime-flip(Should)은 "보유 중" 국면
  전환을 매일 재판정해야 해 미래참조 금지 원칙상 그 날짜까지의 슬라이스로 판정해야 함을
  주의(entries.mjs의 asOfSeries 패턴 재사용). 5.5년 픽스처(US-1)에 하락 구간이 있으므로
  AC3(하락 국면 방어 청산 실제 발동 케이스)는 그 픽스처로 검증 가능.

## Iteration 7 — US-7. 청산 변형 A(국면 조건부) + regime-flip
- 완료 스토리: US-7
- 예상 밖 발견: regime-flip(Should, 그러나 AC2가 요구해 실질 필수)은 "보유 중 매일" 국면을
  재판정해야 하는데, 국면은 유니버스 전체를 봐야 하는 값이라(US-6의 rsPercentileValue와
  같은 계열의 문제) exits.mjs가 개별 티커 series만 갖고 걷는 walkExit 루프 안에서는 원래
  구할 수 없었다. 다만 regime.js의 breadthTimeSeries()(SMA200은 항상 과거창)와
  applyHysteresis()(날짜 오름차순으로 과거 상태만 이어받는 순수 전진 상태기계)는 둘 다
  미래를 보지 않으므로, "유니버스 전체로 한 번 계산한 날짜별 국면 시계열에서 그 날짜를
  조회"하는 것과 "그 날짜까지 매번 새로 슬라이스해서 재계산"하는 것이 수학적으로 동일한
  값을 준다는 사실을 확인 — 이를 근거로 매 청산 스텝마다 재계산하는 대신 딱 한 번(전체
  백테스트당 1회) 계산한 날짜→국면 맵을 공유하는 방식으로 구현(정확성 손실 없이 비용만
  절감 — exits.test.js AC2가 이 인과성을 직접 검증).
- 변경 파일:
  - scripts/lib/exits.mjs: `atrStopTriggered()` 헬퍼로 ATR 손절 판정 추출(exit_stop_atr이
    쓰던 로직을 그대로 함수화 — 재구현 아님), exit_regime_conditional이 재사용. `walkExit`/
    `computeExitPerformance`/`aggregateExitPerformance`에 선택 `context` 매개변수 추가 —
    `computeExitPerformance`는 `record.regime`을 항상 자동으로 context에 병합(호출부 배선
    불필요). `EXIT_RULES`에 `exit_regime_conditional`(신호일 국면 상승→60일 보유 손절 없음
    / 중립·하락→ATR손절) + `exit_regime_flip`(regimeByDate 맵에서 그 날짜가 정확히
    '하락 전환일'(transitionDate===그 날짜)일 때만 청산 — 이미 하락 지속 중인 날은
    재트리거 안 함) 신규 등록.
  - scripts/backtest.mjs: `regimeSeries` import 추가. `buildRegimeDateMap(rawUniverse)` 신규
    — 유니버스 전체로 `regimeSeries()`를 1회 호출해 Map으로 변환(인과성 근거는 위 발견
    참고). `runBacktest()`에서 1회 생성해 `evaluateExitVariants()`에 전달, 거기서
    `aggregateExitPerformance(..., {regimeByDate})`로 배선. EXIT_RULES를 그대로
    `Object.values()` 순회하는 기존 `evaluateExitVariants` 구조 덕에 신규 규칙 2종이
    코드 변경 없이 자동으로 variants[]에 편입(adopted 항상 false도 자동 적용).
  - scripts/lib/exits.test.js: `sliceUniverseAsOf`/`regimeSeries`/`currentRegime`/
    `buildDataset` import 추가. 신규 describe 4개, 총 11개 테스트 — AC1(exit_stop_atr과
    동일한 ATR 픽스처로 상승/하락/중립 3분기 확인 + computeExitPerformance 자동 배선 확인,
    4개), AC2(5.5년 픽스처의 실제 하락 전환일 1건 확인 + 그 날짜까지만 슬라이스한 유니버스의
    직접 계산이 전체 계산과 정확히 일치함을 증명 + checkStop이 전환 당일에만 true/전날은
    false + regimeByDate 없을 때 안전한 false, 4개), AC3(5.5년 픽스처 하락 국면 표본
    138건에서 stopHitRate 84.78% 확인, 1개), 픽스처 전제 확인 1개. **재사용한 스크래치
    검증**: 별도 스크립트로 실제 stopHitRate>0을 먼저 확인한 뒤 테스트에 반영(추측 금지 원칙
    준수).
  - scripts/backtest.test.js: 기존 "청산 변형 5종+정책 3종=11종" 정확값 단언을 "7종+3종=13종"
    으로 갱신(주석 추가, exit_regime_conditional/exit_regime_flip 2종 반영 — 의도된 확장).
- 승인 기준 확인: AC1(신호일 국면별 규칙 분기 — 상승엔 손절 없음, 중립·하락엔 ATR손절) 통과,
  AC2(regime-flip 전환일 판정의 슬라이스 기준 시점 정합성) 통과, AC3(5.5년 픽스처 하락 국면
  방어 청산 실발동 — stopHitRate 84.78%) 통과.
- 테스트 수 변화: 740 → 749 (+9: exits.test.js 19→30 신규 11개 중 기존 backtest.test.js
  정확값 갱신은 신규 테스트 아님 — 스위트 전체 카운트 변화 +9로 최종 확인, 세부적으로는
  exits.test.js +11 신규, backtest.test.js는 항목 수만 갱신되고 테스트 개수 자체는 불변이라
  net +9로 수렴). npm run test(전체 54개 파일, 749개) + npm run build 둘 다 통과.
- 다음 스토리 메모: US-8 청산 변형 C(구조 기반 손절 — 돌파형: 피벗×0.97 / 눌림목형: 눌림
  저점×0.98) + 조합 2종(pullback_resume_vol×exit_structural, entry_close×exit_regime_
  conditional). "진입 유형→손절선 매핑"이 핵심 — entry_close 등 돌파형과 pullback_* 눌림목형을
  구분해야 하므로, 레코드나 진입 변형 자체에 "유형(breakout/pullback)" 태그가 필요할 수 있음
  (지금은 진입 변형 객체에 그런 필드가 없음 — US-8에서 ENTRY_VARIANTS/PULLBACK_ENTRY_VARIANTS에
  타입 메타를 추가하거나 combo 정의 시점에 고정 매핑으로 처리할지 결정 필요). "유형 불명 시
  안전 기본값"도 AC1이 요구 — 예: 판정 불가 시 손절 미가동(시간 청산만) 등 방어적 선택.
  발동률을 1급 판정 재료로 기록(<40% 선커밋 기준, v10 "83.7% 교훈" 참고) — combos[]/변형
  결과에 발동률 필드를 명시적으로 포함할 것.

## Iteration 8 — US-8. 청산 변형 C(구조 기반 손절) + 조합 2종
- 완료 스토리: US-8
- 예상 밖 발견/설계 결정: 지난 스토리 메모가 예상한 대로 "진입 유형→손절선 매핑" 문제가
  핵심이었다 — entries.mjs의 ENTRY_VARIANTS/PULLBACK_ENTRY_VARIANTS 각 항목에 순수 추가
  필드 `type`('breakout'/'pullback')을 붙여 해결(기존 name/description/simulate 변경 없음).
  구조 손절선은 entryIdx까지의 asOfSeries만으로 매번 재계산(재사용: 돌파형은
  entryPoint.js의 computePivot(), 눌림목형은 pullback.js의 judgePullback().stopReference —
  이미 US-5가 눌림 저점×0.98을 계산해 두고 있어 재구현 없이 그대로 재사용). "유형 불명 시
  안전 기본값"은 entryType이 'breakout'/'pullback' 어느 쪽도 아니면(단독 exitVariants 적용
  등) 손절선 자체를 null로 반환 — 손절 미가동, 60거래일 시간 청산만 적용(방어적 선택,
  손절선을 잘못된 유형으로 오판정해 조기 청산시키는 쪽보다 안전).
- 변경 파일:
  - src/lib/constants/entry.js: `PIVOT_STRUCTURAL_STOP_MULT = 0.97` 신규(돌파형 구조 손절
    계수). 눌림목형 계수(0.98)는 이미 v11 US-5의 `PULLBACK_STOP_MULT`로 존재 — 재사용.
  - scripts/lib/entries.mjs: ENTRY_VARIANTS 4종에 `type: 'breakout'`, PULLBACK_ENTRY_VARIANTS
    3종에 `type: 'pullback'` 순수 추가(name/description/simulate 불변).
  - scripts/lib/exits.mjs: `structuralStopPrice(series, entryIdx, entryType)` 신규 —
    entryType 분기(pullback→judgePullback().stopReference 그대로 사용, breakout→
    computePivot().pivot×PIVOT_STRUCTURAL_STOP_MULT, 그 외→null). `EXIT_RULES.exit_structural`
    신규(checkStop이 series/entryIdx/close/entryType만 사용 — 대부분의 기존 규칙처럼 이미
    walkExit이 넘기는 필드 그대로). `computeComboPerformance`는 이제 (1) 진입 변형의
    simulate()에 `record.rsPercentileValue`도 함께 넘김(눌림목 진입 변형의 P1 판정에 필요 —
    이전엔 조합 실험이 돌파형 4종만 다뤄서 누락되어 있었음, 순수 추가라 기존 3개 조합엔 영향
    없음) (2) walkExit에 `{ entryType: entryVariant.type, regime: record.regime }` context를
    항상 넘김(exit_structural·exit_regime_conditional 조합 양쪽에 필요, 대부분의 exitRule은
    무시). COMBOS 배열에 v11 US-8 대표 조합 2개 추가:
    `pullback_resume_vol_x_exit_structural`, `entry_close_x_exit_regime_conditional`.
  - scripts/lib/exits.test.js: COMBOS 개수 단언 3→5(entryVariant가 PULLBACK_ENTRY_VARIANTS
    소속일 수도 있다는 점 반영해 허용 목록도 확장). 신규 describe 3개, 총 10개 테스트 —
    AC1(돌파형/눌림목형 손절선 손계산 4개 + 유형 불명 시 안전 기본값 1개), AC2(진입일 이후
    극단적 스파이크가 끼어들어도 손절선이 재계산되지 않음 확인 1개), AC3(entry_close×
    exit_regime_conditional의 regime 컨텍스트가 조합 경로에서도 배선됨 2개 + 등록된
    pullback_resume_vol_x_exit_structural 조합의 entryVariant 타입 확인 1개 + entryType="
    pullback" 배선을 최소 가짜 진입 변형으로 격리 검증 1개 — pullback_resume_vol 자체의
    체결 로직은 entries.test.js에서 이미 검증되어 있어 여기서 재검증하지 않음).
  - scripts/backtest.test.js: combos 개수 단언 3→5(신규 조합 2개 이름 포함) + 기존
    "청산 변형 7종+정책 3종=13종" 정확값 단언을 "8종+3종=14종"으로 갱신(exit_structural
    반영 — 의도된 확장, evaluateExitVariants가 Object.values(EXIT_RULES)를 그대로 순회하는
    구조 덕에 코드 변경 없이 자동 편입).
- 승인 기준 확인: 1(진입 유형→손절선 매핑 테스트 — 돌파/눌림목 각각 + 유형 불명 시 안전
  기본값 명시) 통과, 2(손절선이 진입 시점 구조로 고정되고 이후 데이터로 재계산되지 않음)
  통과, 3(조합 레코드에 발동률 포함 — aggregateComboPerformance가 이미 US-9부터 fillRate를
  반환하므로 추가 변경 없이 충족 + adopted 전부 false) 통과.
- 테스트 수 변화: 749 → 759 (+10, exits.test.js 신규). npm run test(전체 54개 파일, 759개)
  + npm run build 둘 다 통과.
- 다음 스토리 메모: US-9 청산 변형 E(클라이맥스 부분 청산) — exitSignals.js의 X4(클라이맥스
  런) 발생 시 50% 청산 + 잔여 50%는 60일 만기 보유. US-3의 부분 포지션 성과 원시함수를
  재사용해야 함(재구현 금지). 비교 3자 구도(무청산 vs 전량 청산(v10 결과 참조) vs 부분
  청산) 기록 필요 — 산술 새니티(부분 청산 성과가 무청산과 전량 청산 "사이"에 위치, 가중
  평균 보증)가 승인 기준 3에 명시되어 있으니 픽스처로 직접 확인할 것.

## Iteration 9 — US-9. 청산 변형 E(클라이맥스 부분 청산)
- 완료 스토리: US-9
- 예상 밖 발견/설계 결정: EXIT_RULES/walkExit 체계는 checkStop이 boolean(그날 전량 청산
  여부)만 표현할 수 있어 50% 부분 청산을 이 체계에 넣을 수 없었다 — US-8의 COMBOS와 같은
  이유로 별도 경로(exits.mjs의 독립 함수 2개)로 분리했다. X4 미발생 신호를 별도 분기 없이
  자연스럽게 처리한 점이 깔끔했다: exitEvents=[]이면 computePartialPositionPerformance(US-3)가
  잔여 100%를 그대로 60일 만기로 처리해, 결과가 "무청산"(고정 60일 보유) 경로와 정확히
  같아진다 — if/else 분기 없이 입력(exitEvents 배열 길이)만으로 두 경로가 수렴.
- 변경 파일:
  - scripts/lib/exits.mjs: `findFirstClimaxIdx(series, entryIdx)`(entryIdx+1~+60 내 X4
    첫 발생일 탐색, exitSignals.js의 evaluateExitSignals 재사용 — hasExitSignalCode를 그대로
    호출) + `computeClimaxPartialPerformance(record, priceIndex)`(X4 발생 시
    exitEvents=[{date,ratio:0.5}]+finalHoldingDays=60으로 performance.mjs의
    computePartialPositionPerformance를 그대로 호출 — 재구현 없음) +
    `aggregateClimaxPartialPerformance(records, priceIndex)`(climaxTriggerRate 포함, 표본 0이면
    전부 null). performance.mjs에서 `computePartialPositionPerformance` import 추가.
  - scripts/backtest.mjs: `buildClimaxPartial(outTrendTop5, priceIndex, strategies, exitVariants)`
    신규 — 무청산은 strategies[]의 trend·top5·Out·all·60일 고정 보유(evaluateExitVariants가
    쓰는 baseline60과 동일 정의, 재사용), 전량 클라이맥스 청산은 exitVariants에서 이미 계산된
    'exit_climax'(v10 US-9) 항목을 그대로 참조(재계산 없음 — "v10 결과 참조"), 부분 청산만
    aggregateClimaxPartialPerformance로 새로 계산. `climaxPartial` 필드를 backtest.json 최상위에
    추가(outTrendTop5와 동일 스코프 — entryVariants/combos와 기준선 공유).
  - src/lib/backtestSchema.js: `validateClimaxPartial`/`validateClimaxComparisonEntry` 신규
    (top-level 단일 객체, 배열 아님 — combos/pullbackAxis와 다른 형태). 선택 필드 패턴 동일
    (data.climaxPartial !== undefined일 때만 검증, schemaVersion 4 그대로 유지 — 순수 추가).
  - scripts/lib/exits.test.js: 클라이맥스 스파이크(10일, 100→130) 후 추가 상승(50일,
    130→160) 픽스처로 세 시나리오가 뚜렷이 갈리게 설계(무청산 +60% vs 전량 청산 +30% vs
    부분 청산 +45%). 신규 describe 4개, 총 6개 테스트 — AC1(X4 발생 시 레그 2개(50%+50%)
    손계산 일치, X4 미발생 시 레그 1개·고정 60일과 동일 결과, 2개), AC2(부분 청산 첫 레그
    날짜가 evaluateExitSignals() 직접 판정과 정확히 일치, 1개), climaxTriggerRate 집계(발동률
    50% 확인 + 표본 0 null 처리, 2개), AC3(전량<부분<무청산 순서 확인, 1개).
  - scripts/backtest.test.js: `runBacktest — v11 US-9` describe 신규, 2개 테스트 —
    climaxPartial 스키마 통과 + name/adopted/outDetail/comparison 3키 확인, comparison.
    fullClimaxExit이 variants[]의 exit_climax 항목과 값이 같음(재계산 아님) 확인.
- 승인 기준 확인: 1(부분 청산 수익률이 US-3 손계산 문법과 일치 — X4 발생/미발생 픽스처)
  통과, 2(exitSignals.js 호출 확인 — 재구현 없음) 통과, 3(산술 새니티: 부분 청산 성과가
  무청산과 전량 청산 사이에 위치 — 가중 평균 보증) 통과.
- 테스트 수 변화: 759 → 767 (+8: exits.test.js +6, backtest.test.js +2). npm run test(전체
  54개 파일, 767개) + npm run build 둘 다 통과.
- 다음 스토리 메모: US-10 허들 교집합 축(양 유니버스) — `hurdleIntersection[]`: ★★ ∩
  (Pass / Partial 이상 / Fail) × sample × byHolding × 국면. 판정은 fundamentals.js의 기존
  함수 재사용(재구현 금지), coveredFrom 제약·참고치 라벨 유지. "동일 코드가 NDX·NGX 양쪽
  실행에서 각자 산출"이 승인 기준이므로 v10 US-11이 이미 만들어 둔 "유니버스 하드코딩
  검출 테스트" 패턴을 그대로 재사용해 검증할 것. 콘솔 출력에 NDX vs NGX 교집합 비교표
  (중앙값 1차)도 필요 — PRD §5의 두 판정 행 재료임을 확인.

## Iteration 10 — US-10. 허들 교집합 축 (양 유니버스)
- 완료 스토리: US-10
- 예상 밖 발견/설계 결정: PRD 문구 "(Pass / Partial 이상 / Fail)"이 AC1("각 허들 그룹 합 =
  판정 가능 신호 전체")과 표면적으로 충돌했다 — PRD 본문 전체(§4.4·§7·판정 기준표)는
  일관되게 "★★∩Partial+"(Pass∪Partial 합집합)를 핵심 지표로 인용하는데, 이 합집합은
  Pass와 겹쳐서 "3그룹 합=전체" 산술과 안 맞는다. buildFundamentalAxis의 기존 AXIS_VERDICTS
  (pass/partial/fail, 상호 배타적, US-6부터 이미 확립)를 그대로 재사용하는 쪽을 택하고,
  "Partial 이상"은 그 위에 pass∪partial을 합친 4번째 파생값(`partialOrBetter`)으로 별도
  노출했다 — signalQuality의 'all'(=normal+relaxed 합)이 이미 같은 패턴(배타적 분류 +
  편의상 합계를 같은 배열에 공존)을 쓰고 있어 이 프로젝트 관례와 일치. AC1의 "합" 검증은
  pass+partial+fail(상호 배타적 3분류)에 대해서만 성립하는 것으로 해석하고 테스트도 그
  전제로 작성 — PRD 원문이 100% 명료하지 않은 지점이라 이 해석을 명시적으로 남긴다.
- 변경 파일:
  - scripts/lib/fundamentalHistory.mjs: `classifyRecordsByFundamentalVerdict(fundamentalsData,
    records)` 신규 추출 — buildFundamentalAxis(v9 US-6)가 인라인으로 하던 historyByTicker
    구성 + coveredFrom 산정 + verdict 분류 로직을 공유 함수로 뽑아냈다(재사용, 재구현 아님).
    `buildFundamentalAxis`는 이 함수를 호출하도록 리팩터(동작 변경 없음 — 기존
    fundamentalHistory.test.js 전체가 무수정으로 통과해 회귀 없음을 확인).
  - scripts/backtest.mjs: `HURDLE_GROUPS = ['pass','partial','partialOrBetter','fail']`
    상수 + `buildHurdleIntersection(fundamentalsData, inRecords, outRecords, priceIndex,
    holdingDays)` 신규 — strategyKey='consensus_2star'(★★) × basis='allSignals' 신호를
    sample당 1회만 classifyRecordsByFundamentalVerdict로 분류한 뒤, 국면별로는 이미 분류된
    pass/partial/fail 배열을 regime으로 재필터링만 한다(재분류 없음 — 효율 + 일관성).
    `hurdleIntersection` 필드를 backtest.json 최상위에 추가, fundamentalsData 없으면 null
    (fundamentalAxis와 동일한 하위 호환 원칙).
  - src/lib/backtestSchema.js: `validateHurdleIntersectionItem` 신규(combos/pullbackAxis와
    같은 배열 형태 — climaxPartial의 단일 객체 형태와 다름). **버그 하나 발견 즉시 수정**:
    최초 작성 시 `data.hurdleIntersection !== undefined`만 체크해서, hurdleIntersection이
    null인 정상 케이스(fundamentalsData 미지정)에서도 "배열이어야 합니다" 오류로 스키마
    전체가 깨져 기존 테스트 17개가 한꺼번에 실패했다 — `fundamentalAxis`/`climaxPartial`
    등 다른 nullable 필드가 이미 쓰는 `=== null` 병행 체크 관례를 놓친 것이 원인, 즉시
    `!== undefined && !== null`로 고쳐 전부 복구 확인.
  - scripts/lib/fundamentalHistory.test.js: `classifyRecordsByFundamentalVerdict` 신규
    describe, 3개 테스트 — null 안전성, AC2(coveredFrom 이전·fundamentals 없는 티커 제외),
    AC1(판정 가능 신호가 pass/partial/fail 중 정확히 하나에만 들어감, 상호 배타성).
  - scripts/backtest.test.js: `runBacktest — v11 US-10` describe 신규, 5개 테스트 —
    null-안전성, 스키마 통과 + sample×국면마다 허들 그룹 4종 전부 존재, AC1(partialOrBetter
    = pass+partial 신호 수, 2×3×3=18개 조합 전수 확인), AC2(coveredFrom·note 존재), AC3(NDX/
    NGX 양쪽에서 동일 코드로 독립 산출 — v10 US-11 픽스처 재사용).
- 승인 기준 확인: 1(교집합 귀속 정합성 — 위 설계 결정에 따라 pass+partial+fail=전체로
  해석해 확인) 통과, 2(coveredFrom 이전 신호 제외 + 참고치 note 존재) 통과, 3(양 유니버스
  픽스처에서 독립 산출) 통과.
- 테스트 수 변화: 767 → 775 (+8: fundamentalHistory.test.js +3, backtest.test.js +5).
  npm run test(전체 54개 파일, 775개) + npm run build 둘 다 통과.
- 다음 스토리 메모: US-11 relax_off_in_downturn 앱 반영(승인된 채택 1) — 신호일(최신 데이터
  기준) 국면이 하락일 때만 완화 폴백 신호를 top5·추천 풀에서 제외(추세추종·미너비니 공통),
  상승·중립 국면은 v10과 완전 동일(스냅샷 테스트로 확인). constants changelog에 날짜·근거
  (v10 backtest, relax_off_in_downturn)·조건 문구 기재. 화면2에 하락 국면에서만 보이는
  안내 1줄 추가. 앱 분기가 src/lib에 있으므로 백테스트의 relax_off_in_downturn 변형과
  이중 적용되지 않는지 확인 필요(PRD가 명시적으로 경고한 지점 — "기준선이 이미 규칙 포함
  상태가 됨을 콘솔에 명시"). recommend.js/minervini.js 쪽 국면 조건부 분기 지점을 먼저
  찾아볼 것.

## Iteration 11 — US-11. relax_off_in_downturn 앱 반영 (승인된 채택 1)
- 완료 스토리: US-11
- 예상 밖 발견/설계 결정: 게이트 로직을 recommend.js/minervini.js 양쪽에 각각 인라인으로
  넣지 않고, regime.js에 `gateRelaxedFallbackInDownturn(result, regime)` 순수 함수 하나로
  뽑아 양쪽이 공유하게 했다 — 두 결과 형태({list,relaxationApplied,...})가 이미 동형이라
  가능했던 선택(재구현 없음, 회로 의존성도 없음: regime.js는 recommend.js/minervini.js를
  참조하지 않아 순환 임포트 위험 없음). 이 설계 덕에 "앱 분기가 src/lib에 있으므로
  백테스트가 자동으로 동일 규칙 실행"이 문자 그대로 성립 — backtest.mjs의
  evaluateAsOf()/runSmoke()는 regime을 recommend()/runMinerviniRecommend() 호출에 그대로
  넘기기만 하면 됐고, 이 배선만으로 신호 재현 루프 전체(strategies·variants·regimeAxis·
  entryVariants·combos·hurdleIntersection 등 trend/minervini 신호에 의존하는 모든 집계)가
  자동으로 새 규칙을 반영했다 — 별도의 백테스트 전용 게이트 코드는 0줄.
  regime/insufficientSignal 의미 분리도 중요한 결정이었다: insufficientSignal은 게이트와
  무관하게 "원 신호 강도"를 그대로 보존하고(진단용), 게이트로 인해 실제 화면에 아무것도
  안 보이는 경우는 별도의 `regimeGated` 필드로만 표현 — 두 개념을 섞으면 "완화로 5건 이상
  찾았지만 하락장이라 안 보여줌"과 "애초에 신호가 부족함"이 구분 안 되는 문제가 생긴다.
- 변경 파일:
  - src/lib/regime.js: `gateRelaxedFallbackInDownturn(result, regime)` 신규 — regime이
    'down'이 아니면(null·'up'·'neutral') 완전히 무변경 반환(승인 기준 1의 핵심 보증),
    'down'이고 완화 신호가 있으면 그 항목만 필터링 + `regimeGated:true`.
  - src/lib/recommend.js/minervini.js: `recommend(tickers, config, regime=null)`,
    `runMinerviniRecommend(tickers, regime=null)` — 셋째 인자 추가(둘 다 기본값 null이라
    호출부가 안 바꾸면 완전 무변경, 하위 호환 100%). 반환 직전에
    gateRelaxedFallbackInDownturn을 한 번 통과시킨다.
  - src/lib/__fixtures__/recommend-default-baseline.json: US-8 byte-identical 회귀
    픽스처에 `"regimeGated": false` 필드 1개만 추가(실 데이터로 재계산해 이 필드 외에는
    무변화임을 직접 확인 후 반영 — 재구현 아닌 순수 스키마 확장의 기대되는 결과).
  - src/lib/constants/verification.js: changelog에 2026-07-13 항목 + `relaxOffInDownturn`
    키 추가(status:'채택', basis: v10 backtest 근거, condition: PRD 원문 조건 문구 그대로).
  - src/App.jsx: `regimeInfo` useMemo를 recommend/minervini 호출보다 위로 재배치(선언
    순서 문제 — 값 자체는 그대로) + 두 호출에 `regimeInfo?.regime` 셋째 인자로 전달.
  - src/screens/Recommend.jsx: RegimeBadge 바로 아래에 안내 1줄 추가 —
    `recommendation?.regimeGated || minerviniResult?.regimeGated`일 때만 렌더링(모드
    무관, 화면 최상단 1곳).
  - scripts/backtest.mjs: `evaluateAsOf()`/`runSmoke()`에서 regime을 먼저 계산한 뒤
    recommend()/runMinerviniRecommend()에 그대로 전달하도록 순서 변경. buildPolicyVariants의
    relax_off_in_downturn 정책 변형 extraNote를 "기준선이 이미 이 규칙을 포함해 델타가
    이제 0에 수렴하는 게 정상(이중 적용 아님)"으로 갱신 — PRD가 명시적으로 요구한
    콘솔 고지.
  - src/lib/regime.test.js/recommend.test.js/minervini.test.js: gateRelaxedFallbackInDownturn
    + 양쪽 recommend 함수의 regime 인자 배선 테스트, 총 8개 — 하락 국면에서 완화 신호
    제거(regimeGated:true), 상승·중립·미지정에서 완전 무변경(승인 기준 1), strict-pass는
    하락 국면에서도 안 걸림.
  - src/screens/Recommend.test.jsx: 배너 렌더링 테스트 3개 — recommendation.regimeGated로
    렌더, 미지정 시 비렌더, minerviniResult.regimeGated만으로도 렌더(추세추종·미너비니 공통
    확인, 승인 기준 3).
  - 전체 5.5년 실데이터 기준 backtest.test.js 스위트(84개 테스트)는 이 핵심 로직 변경 후
    전부 무수정으로 통과 — 정확값 단언들이 대부분 "sum=total" 류의 상대적 불변식(국면별
    합=allSignals 총합 등)이라 기준 신호 수 자체가 줄어도 자동으로 성립하기 때문. 실제로
    예상보다 훨씬 파급 범위가 작았던 지점.
- 승인 기준 확인: 1(국면별 분기 — 하락에서 제외, 상승·중립은 v10과 완전 동일) 통과,
  2(changelog 항목 + 조건 문구 존재) 통과, 3(안내 1줄이 하락 국면에서만 렌더링 + 기존
  테스트 전체 통과) 통과.
- 테스트 수 변화: 775 → 786 (+11: regime.test.js +3, recommend.test.js +3,
  minervini.test.js +2, Recommend.test.jsx +3). npm run test(전체 54개 파일, 786개)
  + npm run build 둘 다 통과.
- 다음 스토리 메모: US-12 UI — 거래량 배지 승격(승인된 채택 2) + 상태 0 문구. constants/
  verification.js의 거래량 동반 돌파 확인 규칙 상태를 "조건부 품질 우위 — 체결 거래
  승률 +11.6pt 실측(NDX Out) · 전량 자동 적용 시 기회비용 존재"로 갱신(양면 라벨, v10
  "83.7%" 교훈과 같은 패턴 — 우위/기회비용을 분리 렌더 불가하게). 화면2 양 모드 카드
  상태 2에 "거래량 동반 돌파 확인 ✓/⚠ 미확인" 배지 1급 승격(순위·점수 무변화 — 스냅샷
  검증). (Should) 상태 0 배지 문구 변경 + pullback.js의 재개 트리거가 표시 + "측정 중"
  상태 동반. v10의 "열위" 라벨 렌더링 테스트 문법을 그대로 재사용할 것(구조가 같음).
